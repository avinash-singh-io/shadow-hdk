"""The ports, implemented — each adapter a package of its own, none importing another (a test
holds it). Optional SDKs are extras of the one distribution: `shadow-hdk[langchain]`, `[mqtt]`,
`[otel]`, `[sandbox]`, `[search]`, or `[all]`."""
