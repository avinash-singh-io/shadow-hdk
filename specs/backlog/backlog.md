---
type: Backlog
---

# Backlog

> **Last Updated**: 2026-09-10

---

## Priority Levels

| Level | Meaning |
|-------|---------|
| **P0** | Critical — blocks current phase |
| **P1** | High — address in current/next phase |
| **P2** | Medium — within 2 phases |
| **P3** | Low — nice to have |

**Status**: `open` | `in-progress` | `resolved` | `deferred` | `deprecated`

---

## Bugs

| ID | Title | Priority | Status | Phase | Detail |
|----|-------|----------|--------|-------|--------|
| BUG-001 | A component named like an agent meta-tool is shadowed silently | P2 | **closed 2026-09-10 (`67e2490`)** | 13 | A registration whose id matches a name in `Pattern.meta_tools` (`send`, `spawn`, `release`, `done`, …) never runs: the agent adapter builds the model-facing tools from the meta-tools by name (`adapters/agent/component.py:184`) and the meta-tool answers the call. Observed: a tool `send(to)` got *there is no helper ''; spawn one first*. Fix: refuse at the point the agent builds its tool list, naming both the registration and the meta-tool. |
| BUG-002 | `MqttLink`: concurrent first use opens N broker sessions; a witness sees each publish N times; `close()` leaks all but one | P1 | **closed 2026-09-10 (Phase 16 Group 3, `c0224e4`: one `asyncio.Lock` across the open path and `close()`; `opened == 1` after three concurrent connects, four racing runs received once, no session outlives close)** | 16 | `_ensure` (`adapters/mqtt/link.py`) has no lock around drop-and-open: two callers both see no client and both open one; the last assignment wins and the rest stay connected, subscribed and routing into the same `_on_message`. Measured against the local broker: `gather(link.connect(), link.connect())` → 2 clients, one publish queued twice for a witness, one client still connected after `close()`. `FanOut` runs branches concurrently, so this is the ordinary case. Fix: an `asyncio.Lock` across `_ensure`'s open path and `close()`. See `specs/phases/phase-16-mqtt/design.md` F1. |
| BUG-003 | `MqttLink`: registration races paho's network thread; a raise inside `on_message` ends the thread while the link still reports connected | P1 | **closed 2026-09-10 (Phase 16 Group 3, `c0224e4`: registration under the thread lock, tables before filters, `_open` re-checks, faults counted; proven by a recording lock, a refusing filter set, an injected late registration, a fault in `on_message` and one in `on_subscribe`)** | 16 | `sensor()/witness()/actuator()` mutate `_sensor_filters`/`_witness_filters`/`_ack_filters` (and `_first`/`_queues`) without `_lock`, while `_on_message` iterates them under the lock on paho's thread: `RuntimeError: Set changed size during iteration`, or a `KeyError` on `_first` (set after the filter is added), inside the callback. paho 2.1 re-raises callback exceptions by default (`suppress_exceptions=False`); `loop_forever` returns and the thread dies with `is_connected()` still true — deaf and mute. Same root: a device registered while `_open` is between its subscribe loop and `self._client = client` is never subscribed. Fix: mutate under `_lock`; set `_first`/`_queues` before adding the filter; in `_open` subscribe from a snapshot and re-check after assignment. Design F2. |
| BUG-004 | Lease, `seq` and held children reset on every resume | P0 | **closed 2026-09-10 (`e1d6ef3`, Phase 17 Group 1, D33)** — reproduced first (a 3-step lease admitted 5, `seq` ran 0–6 twice, `steps_taken` said 3); `RunState.spent` carries the meter and the record's mark, `_stream` seeds both from the checkpoint, and the interrupt carries `resume_seq` because the state's mark is written when a node returns. Contract 0.9.0. **The held-children half is BUG-015.** | 6 → 7 | `run` and `resume` both build a fresh `Session`, `LeaseMeter` and `Emitter` (`runtime/loop.py` `_stream`); `RunState` holds handles, observations and iterations only. **Reproduced:** five sequential steps under a three-step lease with an Ask on step 3 — all five ran, `Ended.steps_taken` = 3, `seq` ran 0–6 twice for one run id. A parent parked while holding a child resumes with `children.held == ()` and the child is orphaned. The refusal note's "the bound has to be structural" rests on this bound holding. Fix: meter, seq and held handles ride in graph state under D19; contract minor bump. |
| BUG-015 | A held child does not survive its parent's park; the parent silently spawns another | P1 | **closed 2026-09-10 (`f378d9c`, Phase 18 Group 3, D37)** — `RunState.children` carries what a run holds; a child shares its parent's checkpointer by default; one spawned with its own is named in `children.lost` with the reason rather than replaced by a duplicate; released and ended children stay gone. Contract 0.11.0. | 7 → 17 | `HeldChild` carries a `Composition`, a checkpointer and a `Cancellation`; two of those are objects, not JSON, so D19's state cannot carry them. **Reproduced:** a parent spawns a child, parks on an Ask, resumes with `children.held == ()`, spawns a *second* child (`id-0002`) and leaves the first (`id-0001`) parked forever — no handle, no reachable checkpointer, and its carved lease settled to nobody. Split out of BUG-004 deliberately: the fix is a decision about what a held child *is* across a restart (a handle plus a host-supplied checkpointer? a child that re-registers itself?), not a field. |
| BUG-005 | The assistant's tool calls are dropped from the transcript | P0 | **closed 2026-09-10 (`b36dada`, Phase 17 Group 2)** — `Message.tool_calls`; the agent records them; the LangChain adapter builds them, carrying non-mapping arguments under one key. It survived seventeen phases because every agent test used `ScriptedModel`, which never checks the pairing rule; a live multi-turn test now covers it. Contract 0.10.0. | 0 → 1 | `kernel/ports.py` `Message` has `role`, `content`, `tool_call_id` and nothing for an assistant's calls; `adapters/agent/component.py:148` appends the text and discards the calls; `adapters/langchain/model.py:141` emits a bare `AIMessage` followed by `ToolMessage`s. OpenAI and Anthropic reject a tool result with no preceding call, and even a lenient provider never shows the model which tool it called with what arguments. Every agent test uses `ScriptedModel`; `test_live.py` is single-shot; the only multi-turn wire evidence is a lenient router (`examples/real.py`). Fix: kernel minor bump adding `tool_calls` to the assistant message, carried by the LangChain adapter; one live multi-turn test behind `-m live`. |
| BUG-006 | Resume over the wire always raises; `serve` has no trust boundary | P0 | **closed 2026-09-10 (`1f44578`, Phase 17 Group 3, D34)** — a session owns a checkpointer so a resume works and keeps its lease; `initialize` required for run *and* resume; an omitted version is a mismatch; every runtime→host callback has a finite default timeout naming the method; `serve` is loopback-only without a `token=`; `wire.md` corrected on the run token, the handshake and HTTP/2. | 9 | `wire/sides.py` `_drive` builds `RunOptions` with no checkpointer, so every `resume` is `ValueError: resume needs options.checkpointer` and an Ask parked over the wire is unrecoverable; no test covers it. `wire/serve.py` `open_session` issues a session on a bare GET with no credential — the run token `wire.md` lists under *rules already fixed* is unbuilt (phase-9 tasks say so). `_initialize` defaults an omitted version to a match and is never required before `_run`. No callback has a timeout, so a host that hangs in `judge` or `complete` stalls the run past its wall ceiling. `wire.md` says HTTP/2; uvicorn serves HTTP/1.1. Fix: a checkpointer per session or drop the method until it has one; loopback-only until the token lands, and say so; require `initialize`. |
| BUG-007 | mypy strict silently skips `wire`, `contained`, `derivation`; nine errors in `wire` today | P0 | **closed 2026-09-10 (`8a9e566`)** | 9 → 12 | `pyproject.toml` `[tool.mypy] mypy_path` omits the three packages; each ships `py.typed`, so mypy resolves them through the editable install's `.pth` entries and silences them as site-packages. "Success: 112 files" is the kernel plus whatever the tests import. **Reproduced:** `MYPYPATH=packages/wire/src:packages/adapters/contained/src:packages/adapters/derivation/src uv run mypy --no-incremental` → 9 errors in `wire/remote.py:62,136` (arg-type), `wire/context.py:95` (no-any-return), `wire/sides.py:233` (var-annotated, arg-type), `wire/sides.py:75-77,249` (unused-ignore). Fix: add the paths; fix the nine. |
| BUG-008 | Workspace escape via hard link; a NUL byte in a path raises out of `invoke` | P1 | **closed 2026-09-10 (`7fe2b0a`, Phase 18 Group 1)** — reproduced first (`read_file` returned the outside content and `write_file` **overwrote** it); the filesystem is asked what a file is rather than what it is called, a NUL byte is data, `adapters.md` corrected | 3 | `adapters/workspace/components.py:72` resolves symlinks, but a hard link *is* the outside inode: `os.link(outside, root/"hl.txt")` let `read_file` return the outside content and `write_file` overwrite it (reproduced). With the subprocess sandbox (`run_shell: ln ~/.zshrc x`) this is a two-component escape with no rule violated. `{"path": "a\0b"}` raises `ValueError` past the `except` at :124-128 — a contract breach. TOCTOU between `_resolve` and `write_text` (:145). `adapters.md` says `write_file` is `contained=True`; the code says `False` (:35). Fix: `lstat` and refuse `st_nlink > 1`, or open `O_NOFOLLOW` and compare `st_dev/st_ino`; catch `ValueError`. |
| BUG-009 | The subprocess leash keeps `HOME`, sets no rlimits, and orphans process trees | P1 | **closed 2026-09-10** — the leash half in `7fe2b0a` (D35: own session, the group killed on return, `HOME` gone, `TMPDIR` in the workspace, `memory_mb` real via `prlimit` where the platform enforces it, output capped as it arrives) and the **containment proof** in `69bf520` (D36: a proof is a capability test the sandbox runs through `wrap`, `declared` is a claim and never evidence, inconclusive is not a denial, and trust covers an unknown but never a fact). The fake-`runsc` reproduction now ends in `NotContained`. | 3 → 11 | `runtime/leash.py:20` keeps `HOME` in `KEPT_ENV`, so a model-written script reads `~/.ssh` or removes the home directory while the profile declares `writes: {workspace}`; `:51` `kill()`s the direct child without `start_new_session` — a backgrounded `sleep` survived the step (reproduced). No memory or CPU limit; `adapters.md:166` documents a `memory_mb` that does not exist. `communicate()` buffers all output before the cap (:49,:54), so `output_limit` bounds the observation, not RAM. The contained proof (`adapters/contained/backends.py:60-96`) is a string match on `dmesg`/DMI: a five-line fake `runsc` on `PATH` passed it, and a harness host that is itself sandboxed (Cloud Run, Fly) satisfies it falsely. Fix: new session + `killpg`; drop `HOME`; rlimits; a capability test for containment or a declaration with a warning. |
| BUG-010 | `Await` and resume invoke the component twice; the re-run judgement can override the human's answer; two Asks in one `FanOut` resume one at a time, re-running the answered branch | P1 | **closed 2026-09-10** (D38, contract 0.11.0 → 0.12.0) | 6 → 7 | All four parts reproduced before anything changed. A step that parked is no longer judged again and an `Await` is no longer invoked again: `_stream` reads the checkpoint's pending interrupts (it already did, for D33's `resume_seq`) and the resumed leg resumes **at** its interrupt. One answer settles every step that asked; an answer keyed by step id addresses them individually. An `Ask` is answered with a `Judgement` — an object in process, its JSON over the wire, loaded at the runtime's edge (D19) — so the human's answer decides and a bare value is refused rather than treated as consent nobody gave. **The row's fan-out claim was wrong**: the run does not end `failed` with LangGraph's message; it parks again, and a caller can answer both one at a time, re-running work each time. The idempotency key stays where it was — the harness no longer manufactures the double, but a device or a network still retries on its own. |
| BUG-011 | The ACP purse double-charges cumulative usage per step; `stop()` has no kill fallback | P1 | **closed 2026-09-10** (`64e74e8`, Phase 18 Group 5) | 4 | Three parts, each reproduced first. **The purse charged every turn again**: `Spend` accumulates because a session does, `_usage` handed the meter the running total, and `step.py` adds per step — measured, three 100-token prompts charged 600. `Spend.take()` is now the only way the meter reads it and answers *since you last asked*. **Cents are the difference of the rounded totals**, so a sub-cent charge stays in `amount` and is charged on the turn the total crosses a cent — the class's own *two hundred of them are eighty cents* is a test now. **This diverges from the row's proposed fix** of `None` below a cent: `None` means unknown and a meter charges nothing for it, so the money would have been lost as before, only relabelled. `cents` asks `cost_seen` rather than `amount == 0`, because a provider that priced a turn at nothing has told us. **`stop()`** asks, waits out a grace period, then ends the **group** — the rule moved into `runtime/processes.py` rather than being written twice, and the ACP child gets `start_new_session=True` for the same reason a leashed program does (D35). 21 tests; 18 mutations, all bite |
| BUG-012 | `Pattern.ceiling` never read; the skill check never called; `compose` results never reach the model; usage lost when the model raises | P1 | **closed 2026-09-10** (`2ed4514`, Phase 18 Group 5) | 8 | Five promises, all kept now, all reproduced first. **`compose`**: results were matched to calls by `call.id`, which cannot work for a plan whose step ids are the model's own — and `compose` being a meta-tool meant the call was skipped and left **unanswered**, the dangling tool call every provider rejects (BUG-005's rule). Four of six shipped patterns are built on it. Answered now with every step by name, including steps that did not run. **Usage on a provider exception**: measured, two paid turns (2000 tokens, 14 cents) recorded as zero; it ends `Completed` with a `reason` and its spend, beside `lease_exhausted` and `out_of_turns`, and the catch is `Exception` so a `CancelledError` still stops the run. **`Pattern.ceiling`**: enforced at judgement and absent from the catalogue, only ever narrower, with the deployment asked first so its own words are what a person reads. **`skills.missing_for`**: `AgentComponent` takes a skill, checks it against `visible()` before the model is called at all, and joins its procedure to the role prompt. **`widens()`**: ask lines compared as well as ceilings — a set with the house's ceiling and no ask line used to pass, which is a team deleting every approval and being told it changed nothing. 27 tests; 20 mutations, all bite |
| BUG-013 | Derivation: the evaluator raises out of `invoke`; the fingerprint is not canonical; floats enter | P2 | **closed 2026-09-10** (`8ea82ad`, Phase 19 Group 2) | 12 | All four parts reproduced first, and **the row corrected in three places**. **Total**: `values.py` opened with *No operation raises on its inputs* and `_fix` broke it — `1e22`, `Infinity` and the product of two values that each fit raised `InvalidOperation` through `evaluate` and out of `invoke`, past D7's boundary; `evaluate` answers `out_of_range` now, naming the value. A **`NaN` literal does not raise**, where the row implied it did. **Canonical**: magnitudes go through the same quantizer the arithmetic uses, in the ground's tree and the table's cells, so `"1"`, `"1.0"` and `"1.00"` are one identity. **Normalised — and the row had this in the wrong place**: NFC versus NFD makes no difference in a *unit* and a decisive one in a **cell**, where it is not a hashing problem but a **wrong count** (1 and 0 for the same word). Both sides normalise now. **Typed**: `Table.rows` has said `Mapping[str, str | bool]` since Phase 12 and nothing enforced it; refused rather than coerced, naming the row, the column and what to send. The measured damage was narrower than filed and still real — the arithmetic survived (`0.1` and `"0.1"` both sum to `0.300000000000`) but the fingerprint did not. **Two existing tests encoded the weaker property** (that the canonical tree echoes the caller's spelling) and were rewritten. **Unit cancellation deliberately deferred**: unit algebra is a design question about what `kg·m/s²` means to this engine, not a defect in what it does. 14 tests; 13 mutations, all bite |
| BUG-014 | `FileSink`: a short write leaves a torn line silently; a torn tail plus a later append makes the file unreadable | P2 | **closed 2026-09-10** (`b87de9f`, Phase 19 Group 2) | 14 | Both halves reproduced. **A short write was silent** — measured, `propose` returned normally having written half a record; every byte is written now or it raises saying how far it got, and a partial write continues rather than failing because a signal or a full pipe is ordinary. **A torn tail plus a restart destroyed the record** — the next process opened `O_APPEND` and wrote onto the end of the half-written line: measured, three lines on disk, `p.jsonl:2 is not a proposal`, and two good proposals unreachable. The file is cut back to its last complete line before anything is appended, which is not data loss because `propose` had never returned for it. **This retires Phase 14's equivalent-mutant claim** that *a torn line can only be the last* — true within one process, false across a restart, and a restart is the only time one exists; Phase 14's `fsync` -is-per-inode claim still holds and is cited. Also: `write`/`fsync` moved off the event loop (asserted by which thread synced, not by a stopwatch); the directory synced once when the name is created; a context manager with idempotent `close`. An existing test failed correctly and was split into two claims. 13 tests; 11 mutations, 10 bite, 1 **equivalent and named in the code** |
| BUG-016 | The runtime's per-step overhead is over D11's budget: 1.36 ms/step against ≤ 1 ms, and 2.4× the figure recorded in the benchmark | P2 | **closed 2026-09-10** (`3ffbde2`, Phase 19 Group 1) | 0 | Reproduced first: a 100-step run built **101 TypeAdapters**, one costs **0.743 ms**, and the 101 of them were **75 ms of a 132 ms run — 57% of the runtime's entire per-step overhead**. `contracts.py` built a fresh adapter on every `dump`, `load`, `round_trip` and `json_schema`, and D19 means every step dumps its observation through there. The adapter is a pure function of the type, so one dict keyed on the type. Re-measured: **0.594 ms/step**, inside D11's ≤ 1 ms — the drift was entirely this, and the 57.0 ms recorded at founding was the design's true cost all along. **D11's 3× slack was deliberately not tightened**: the runner measures 2.1× this machine and the regression was 2.4×, so the ranges overlap and no stopwatch threshold separates *slower code* from *slower hardware*. What guards this class instead is a claim about the mechanism — a run builds one adapter per contract type — which is the same number on every machine. 8 tests; 7 mutations, all bite. **CI went green for the first time in this repository's history**: 123.9 ms on the runner where it had been 392.7. || BUG-017 | `served_over_http` prints a `CancelledError` traceback on a clean exit | P2 | **closed 2026-09-11** | 3 | Found while driving the wire from an ordinary program rather than from a test. The context manager cancelled uvicorn mid-`serve`, so uvicorn's own tasks were torn down by force and one surfaced the cancellation as an **unretrieved exception** — a full traceback on the way out of a block that had done nothing wrong, and the first thing anyone driving the wire sees. **Every wire test missed it** because pytest's anyio runner owns the loop and absorbs it; it is only visible to a program that calls `asyncio.run` itself, which is every program that is not a test. The regression test therefore runs a listener in a **subprocess** and reads what the process wrote. The first fix passed for the wrong reason and was replaced: it waited on `server.started`, which uvicorn sets once and never clears, so the loop was a five-second sleep that happened to outlast the shutdown — 26.3 s for three tests against 5.8 s once it waited on `serve` returning. 2 mutations, both bite. |
| ENH-004 | `Ports.model` is required, so the no-model case has to lie to the type checker | P3 | **closed 2026-09-11** (Phase 20) | 1 | The runtime is a governed workflow engine before it is an agent runner: a composition of plain components runs with no model, and `Ports(model=None, ...)` works at runtime because nothing calls it. But `Ports.model` is typed `ModelPort` and the docstring says *only `observer` is optional*, so a host writing the simplest possible composition gets an error from `mypy` for a program that runs. Found while writing the README's smallest working example. `model: ModelPort \| None = None` is the fix and it is a **contract change** — it waited for a version carrying others, and 0.14.0 is it. `kw_only` so the field takes a default without reordering the five that follow; every caller already named it. Forced by the coder example, where a provider owning its own loop leaves `model` `None` legitimately: what had been a documented wart became an obstacle the moment `examples/` came under `mypy`. |
| BUG-018 | The subprocess sandbox and the ACP `execute` kind declare `reads`/`writes` as `{workspace}` however uncontained they are, so governance judges them on a false profile | **P0** | **closed 2026-09-11** (Phase 20) | 7 | **Found by running the coder example.** Asked for a landing page, the agent called `run_shell` and the output carried `specs/status.md`, a listing of the repository's *parent* directory, files from unrelated projects, and `/Applications`. Nothing refused it, because nothing had been told there was anything to refuse. A plain subprocess honours `cwd` and nothing else — `cd ..` works, an absolute path works — so the narrow claim was false, and a mode permitting `writes: {workspace}` was in fact permitting writes anywhere on the machine while the record said the workspace. **The same function got `reaches` right and said why in its own docstring** — *a governance system fed a lie is worse than one fed nothing* — so the rule was written down and then applied to one field of three. The identical mistake sat in the ACP adapter's `execute` kind; neither imports the other, which is why it had to be fixed twice and why the rule is a test in both places now rather than a habit. Proven rather than argued: two tests read and write outside the workspace and assert that they could. Consequence, and it is meant to be felt — a host permitting an agent to run code on an ordinary machine now has to write `everything`, because that is what it is granting. Narrowing it back is what containment is for (D25, D36). |
| BUG-019 | A provider's child process outlives the session that spawned it | P1 | **closed 2026-09-11** (`cbc1ff9`, Phase 23 Group 4, D53) | 0 | Two `claude -p` children from the coder example were found alive **ten hours** after their sessions ended, each holding a subscription seat and a relay socket. Both were launched with `--mcp-config` naming the registry relay, so they are ours. `JsonlSession.close()` does end the group, but the example reaches it only through `async with` on a clean `return`; a `KeyboardInterrupt` arriving inside the blocking `input()` — the ordinary way a person leaves a REPL — is not shown to reach it. Not reproduced under a test yet, and the fix is not the example's: D35 says a step owns the process tree it starts, and a *session* the host holds across steps is the same obligation one level up — the environment (Phase 22) or the host contract (Phase 23) must make a provider's process die with the thing that opened it, whatever way that thing ends. Until then: `ps -eo pid,etime,command | grep 'claude -p'` after a session. |
| BUG-020 | An `Ask` raised inside an agent's tool call never reaches the host; the model is told the step did not run | P1 | **closed 2026-09-11** (harden-to-24, D57) | 23 | Found by the host example. An agent's tool call is a child run (D51); when the policy answers `Ask` for it, the child parks and `carry_out` sees no `observed` for that call, so the model is told *"that step did not run"* and continues — measured: the scripted worker went on to `propose` and `done` claiming it had written the file it never wrote, and the run ended `completed`. The parent run never parks, so a host watching for `Asked` at the top sees nothing to answer, and the parked child is released with the run. Consent-before-effect is what an Ask is for, and inside an agent it is currently a silent no. The fix is not the example's: either the agent returns `Asked` itself and resumes where it parked (its transcript would have to survive the park — D38's problem one level up), or the runtime lets a host answer a *child's* question directly (the `Asked` event carries the child's run id and handle; `resume` would need the child's composition, which the `Spawned` event does not carry). Until then: at the top level an Ask parks, persists and resumes (proven in `tests/test_the_host_example.py`); inside an agent, a policy that would ask should refuse. **Closed by D57**: a component may answer `Asked` and the run parks on it as if governance had asked; on resume it finds the answer and what it kept, both riding the interrupt payload the checkpointer holds (so does what the step was holding — a child spawned and parked in the same step is not in the state's channel, because the node never returned). The agent adapter keeps its transcript, answers `Asked` for a held child's question, and on resume sends the judgement into the child and continues its turn — allowed, the tool runs and the model hears the result; refused, the model is told refused, never *did not run*; two levels down works the same way; a plan whose second step also asks parks a second time. Measured on the host example: the write inside the worker asked the person and ran on `y`. `keep`/`resumed` cross the wire. Seven mutants on each half. |
| ENH-005 | Codex has no strict MCP mode: the user's own servers join a governed run | P3 | open (found 2026-09-12) | — | Measured on `codex-cli 0.154.0`: `-c mcp_servers.<name>…` adds this run's registry, and `default_tools_approval_mode="approve"` pre-permits it, but nothing removes the servers in the person's `~/.codex/config.toml` or a plugin bundles (`cua_repl`, `node_repl` here). `mcp_servers.<name>.enabled=false` works for a config-file server and breaks the bootstrap for a plugin-bundled one (*invalid transport*), and the harness will not read a person's configuration to enumerate them. Claude Code has `--strict-mcp-config`; Codex needs the equivalent, upstream. Until then a Codex run carries the user's own servers, ungoverned, and the converse step's declared effects (reads everything, reaches) say so. |
| BUG-021 | A subscription provider's tool call that the policy asks about is told "the call produced no observation"; the person is never asked | P1 | **closed 2026-09-12** (harden-to-24, D58) | — | BUG-020's sibling on the socket path. The RecordingServer ran a CLI's tool call as a nested run; when the policy answered `Ask`, that run parked, the call had no observation, and the CLI was told an error while nobody was asked — measured with Claude Code in `full` mode. The run cannot park the way D57 parks an in-process agent: the step holding the conversation is what keeps the CLI alive. **Closed by D58**: a `Questions` handle the host keeps (the same kind of thing as `Cancellation`), `ctx.ask()` that puts `Asked` on the record and waits on it, and the RecordingServer resuming the nested run with the host's answer while the CLI waits on the call. With no handle, the call is refused and says nobody was there. The coder answers at the terminal; its `full` mode asks before every write. Measured live: Claude Code's `write_file` waited on a `y` and ran. Crosses the wire (`context.ask`); the runtime side of the wire owns the handle. |

## Features

| ID | Title | Priority | Status | Phase | Detail |
|----|-------|----------|--------|-------|--------|
| _(none)_ | | | | | |

## Tech Debt

| ID | Title | Priority | Status | Phase | Detail |
|----|-------|----------|--------|-------|--------|
| TD-001 | Observations ride in graph state as our own classes | P2 | **closed 2026-09-10 (D19)** | 7 → 9 | LangGraph 1.2 warns on deserializing `shadow_hdk.kernel.observations.*` from a checkpoint and says a future version will block it. **Measured 2026-09-10:** `LANGGRAPH_STRICT_MSGPACK=true uv run pytest -q` is green over 353 tests, so nothing breaks today. Two remedies: a host builds its checkpointer's serializer with `allowed_msgpack_modules` naming our module — which pushes our internals into every host's setup — or `RunState.observations` holds plain JSON and the runtime loads it back, which keeps the state boundary honest. The second is the design answer; done in Phase 9 Group 0 as **D19** — a checkpoint *is* a wire, so the state holds JSON and the runtime loads at its edge. **Verified:** `LANGGRAPH_STRICT_MSGPACK=true uv run pytest -q -W default` now reports zero such lines across 406 tests. |
| TD-002 | `MqttLink`: the witness queue is unbounded | P2 | **closed 2026-09-10 (Phase 16 Group 3, `c0224e4`: `witness(keep=)`, the oldest dropped and counted, `dropped_before` on the next act)** | 16 | `_queues[filter]` is a `deque()` with no `maxlen`; a wildcard topic chattier than the agent's invocation rate grows memory for the life of the link. Proposed: a `maxlen` per witness and a dropped counter reported as data on the next `overheard` — a late reading is data, so is a lost event. Design F3. |
| TD-003 | `wire` imports `adapters.basic.SystemClock` undeclared; intra-workspace dependencies unbounded; 11 adapters ship no `py.typed` | P1 | **closed 2026-09-10** (`c5c1dc6`, Phase 18 Group 5) | 9 | All four are invisible inside the workspace, so every test reads the **shipped metadata** and the marker was verified by opening a built wheel. `SystemClock` moved to `shadow_hdk.runtime.clock` — below both, the remedy the stands-alone rule already names — and `adapters.basic` re-exports it, so no import breaks. A new `tests/invariants/test_a_wheel_carries_what_it_needs.py` walks the wire and asserts it covers every package on disk rather than every package somebody remembered (BUG-007's shape). All seventeen distributions pin `==0.12.0`, since D9 moves them together and the wheels must say so. Twelve `py.typed` markers added, each inside the package it marks. Measured from the artifact: `shadow_hdk_adapters_mqtt-0.12.0-py3-none-any.whl` contains `shadow_hdk/adapters/mqtt/py.typed` and requires `shadow-hdk-kernel==0.12.0`. 8 tests; 5 mutations, all bite — one survived first because the pin check compared whole strings and let `>=0.12.0` through |
| TD-004 | The contract suites run against 7 of 14 adapters | P2 | **closed 2026-09-10** (`f2b1f20`, Phase 19 Group 3) | 8 → 16 | **The wiring was not the fix; the invariant is.** `tests/invariants/test_every_port_is_held_to_its_contract.py` scans the packages for port implementations and requires each to appear with the test module that contracts it — **checked to really contain that contract, not merely mention one** — or a reason it does not apply. The scan found **29** implementations, more than the fourteen adapters, because the runtime's testing doubles are ports hosts depend on. Five adapters wired (agent, contained, derivation, devices, otel) plus `Controlled`, `RuleGovernance` and `Mailbox`. **`mqtt` and `recording` implement no port at all**: a link is a transport the devices adapter speaks over, and the recording server exposes the registry outward. **The suites found no defects — a result, not a non-result** — so the net was widened while being cast: a known id called with inputs of the wrong shape (`None`, a string, `[]`, wrong fields) must be an observation and not an exception, which is D7 from the commoner side. Eleven component ports answer it. 52 tests; 11 mutations, all bite |
| TD-005 | Nothing bounds growth: the plan cache is keyed on full JSON and never evicts; emitter, wire and witness queues are unbounded; a blocking observer holds `run()` open | P2 | **closed 2026-09-10** (`f2b1f20`, Phase 19 Group 3) — and two of its three claims were stale | 0 → 9 | Group 1's test applied rather than a uniform rule: **does the key set grow with load?** **The plan cache does** — keyed on the full JSON of a composition, and a model authors a new one most turns; measured, 2000 distinct compositions gave 2000 entries and ~6.3 MiB that nothing released. Now 512, least-recently-used, evictions counted; safe for a reason that does not generalise, since a plan is pure and an evicted one costs a rebuild. **The observer queue does** — measured, a slow observer handled **1** event while **49,999** queued; bounded at 4096 and it **drops the oldest and counts**, because D11 puts the observer off the critical path and a bound that blocked would move the failure from the broken exporter to the working runtime (TD-002's shape, for the same reason). **The caller's stream does not and must stay unbounded** — filled and drained by the same task, so a bound would deadlock a run against itself; argued in the code and asserted. **Two claims in this row were stale**: the witness queue is already bounded (Phase 16 fixed it under TD-002 and its docstring says so), and the wire has no queues at all |
| TD-006 | Governance and port handling leak outside the governed step | P2 | **closed 2026-09-10** (`1aeda06`, Phase 19 Group 4, contract 0.12.0 → 0.13.0) | 0 → 13 | Six claims, one root cause. **Every component adapter catches `Exception`** — D7 says it should — and `RuntimeStop` **was** an `Exception`, so a lease that ran out, a host that cancelled and a port that broke were all caught by whatever component was running and returned as its own `Failed`. `RuntimeStop` is a `BaseException` now, the same argument that moved `asyncio.CancelledError` out of `Exception` in 3.8; it fixes the inversion in adapters nobody has written yet, and a mutation proved the explicit `except PortFailure` in `step.py` was already dead code. The four reachable sites: `visible()` judged unwrapped (a policy that was down became *that component failed*); `propose()` announced before the sink took it (a sink that raised left a record of a proposal that never arrived); a component port that would not answer was swallowed into `unreachable`, which grep says nothing read (**a host can read it now; putting it on the event stream wants a twelfth event kind and is filed**); and the lease was charged before the judgement, so five refusals under a ceiling of three ended `lease_exhausted`. The step's docstring said seven moves and `runtime.md` a different seven — it does **nine**. **An existing test asserted the inversion while citing D7**, and was rewritten. 9 tests; 9 mutations, all bite |
| TD-007 | Posture is self-declared with a `controlled` default; the lease-at-the-act and the receipt exist only in the devices adapter | P2 | **part closed 2026-09-10** (`b08988b`, Phase 19 Group 4) — **the rest is the owner's under ADR-1** | 13 → 15 | **Closed here, because it is unambiguous:** `context_for` wrote `posture` and `component` over whatever the host put in the run's context, silently, on every step — in the one message governance ever sees. Overwriting is right (D30: a value the host could set is a value a driver could influence); being silent was not. Both names are refused at the door now, each by name. **Open and the owner's:** whether an `Acted` receipt should be required of any step whose profile is irreversible, whichever port it came through, and what a `controlled` posture may assert. Today `exhausted()`, `grounds()` and `Acted` are called only inside the devices adapter, so a `writes: {world}` component registered through callables, MCP or the wire acts with no exhaustion check and no receipt — recorded in the phase history with what it would take, and **not invented here** |
| TD-008 | The constitutional specs, status and roadmap each describe a different day; the ADR directory is empty | P2 | **closed 2026-09-10** (`e85715c`, `b08988b`, `c658d2a`, Phase 19) | 9 → 16 | **Fixing the items fixes a day; the file that fixes the class is `tests/invariants/test_the_documents_describe_this_tree.py`** — three mechanical rules: a repo path a document names must exist, the adapter listing must match `packages/adapters/`, and the runtime module table must match the tree. Placeholders are excluded, or the rule would be switched off within a week. **What it found:** `CLAUDE.md` pointing at a developer guide never written and at a benchmarks directory under `tests/` that does not exist; `file-structure.md` naming four directories that are really `modes/` and `contained/` and TOML inside the agent adapter, while omitting `devices/` and `mqtt/`; and `runtime.md`'s table naming **thirteen of twenty** modules — the seven missing being exactly those that arrived after it was written. `compile.py` said subgraphs *arrive in Phase 6* for six phases after they did. `.coverage` untracked, `context_for`'s silent overwrite fixed (TD-007), and `specs/decisions/` now maps all thirty-eight decisions with its own invariant. **Three of this row's own claims were already false:** `adapters.md`'s `memory_mb` is correct and its `write_file` `contained` was fixed in Phase 18, and `wire.md`'s transport and run token were corrected in Phase 17. **One does not reproduce:** `recursion_limit = max_steps × 4` never bit at depths 40, 80 or 150 with the ceiling set exactly to the steps needed — at 150 the run fails on JSON serialisation of a 150-deep structure, which is a different thing and not a workload. **`.githooks` failing open is deliberate and recorded**: a hook that blocked every commit on a machine without `node` would be worse than one that warns, and since this phase CI runs on every push, so the hooks are belt-and-braces rather than the enforcement. 9 tests; 9 mutations, all bite |
| TD-009 | CI has never run on any phase commit; coverage is gated on the runtime only; the live sandbox proofs run nowhere | P1 | **closed 2026-09-10** (`8a78177`, Phase 18 Group 5) — the pull request is the owner's | 0 | The trigger is widened: every push plus `workflow_dispatch`, so the next push to a phase branch is the first time this repository has been checked by anything but the laptop it was written on. **The pull request is prepared and deliberately not opened** — `specs/adhoc/TD-009/` holds the body and the exact `gh pr create` command. One pull request, not eighteen: the stack is linear, so every phase branch is an ancestor of the top. Against `staging`, which is the staging-first rule and puts the first CI run somewhere a failure is free. The record says what to expect from a red: the 90% coverage floor has never been measured off this machine, the test skipped here for want of `prlimit` will actually run on Linux, and the ten `live` tests stay deselected |

## Enhancements

| ID | Title | Priority | Status | Phase | Detail |
|----|-------|----------|--------|-------|--------|
| ENH-001 | A sensor over a wildcard filter cannot say which topic it read | P2 | **closed 2026-09-10 (Phase 16 Group 3, `c0224e4`: a sensor names one topic)** | 16 | `_latest` is keyed by the filter and `Reading` carries no topic, so `plant/+/temp` collapses N thermometers into one *latest* with no name. Either a sensor names one topic (as a command topic already must) or `Reading` grows a topic field — a contract change for every protocol. Design F4. |
| ENH-002 | TLS on `MqttLink` | P3 | open (found 2026-09-10) | `[~]` | The dev broker has no TLS listener, so it cannot be proven here; a `tls: ssl.SSLContext \| None` option on `_open` lands `[~]` naming a TLS broker as what settles it. Credentials stay R9's. Design F9. |
| ENH-003 | A protocol-adapter contract suite over the device contract | P2 | open — **deliberately deferred** to the first OPC-UA or ROS 2 work: with one implementation, parametrising the MQTT tests over a device factory is a rename, not a contract; the second adapter is what would prove the seam | before OPC-UA / ROS 2 | The environment-varied claims in `tests/adapters/mqtt/test_environment.py` (not there, refuses, leaves and returns with nothing sent late) and the role claims in `test_mqtt.py` are what OPC-UA and ROS 2 must prove too. Parametrise them over a device factory plus an environment handle the way `tests/adapters/contract` does for ports, so each `[~]` row is a fixture rather than a rewrite. Design §7. |
