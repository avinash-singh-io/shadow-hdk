---
type: Backlog
---

# Backlog

> **Last Updated**: 2026-09-10

---

## Priority Levels

| Level | Meaning |
|-------|---------|
| **P0** | Critical — blocks current phase |
| **P1** | High — address in current/next phase |
| **P2** | Medium — within 2 phases |
| **P3** | Low — nice to have |

**Status**: `open` | `in-progress` | `resolved` | `deferred` | `deprecated`

---

## Bugs

| ID | Title | Priority | Status | Phase | Detail |
|----|-------|----------|--------|-------|--------|
| BUG-001 | A component named like an agent meta-tool is shadowed silently | P2 | open (found 2026-09-10) | 13 | A registration whose id matches a name in `Pattern.meta_tools` (`send`, `spawn`, `release`, `done`, …) never runs: the agent adapter builds the model-facing tools from the meta-tools by name (`adapters/agent/component.py:184`) and the meta-tool answers the call. Observed: a tool `send(to)` got *there is no helper ''; spawn one first*. Fix: refuse at the point the agent builds its tool list, naming both the registration and the meta-tool. |
| BUG-002 | `MqttLink`: concurrent first use opens N broker sessions; a witness sees each publish N times; `close()` leaks all but one | P1 | **closed 2026-09-10 (Phase 16 Group 3, `c0224e4`: one `asyncio.Lock` across the open path and `close()`; `opened == 1` after three concurrent connects, four racing runs received once, no session outlives close)** | 16 | `_ensure` (`adapters/mqtt/link.py`) has no lock around drop-and-open: two callers both see no client and both open one; the last assignment wins and the rest stay connected, subscribed and routing into the same `_on_message`. Measured against the local broker: `gather(link.connect(), link.connect())` → 2 clients, one publish queued twice for a witness, one client still connected after `close()`. `FanOut` runs branches concurrently, so this is the ordinary case. Fix: an `asyncio.Lock` across `_ensure`'s open path and `close()`. See `specs/phases/phase-16-mqtt/design.md` F1. |
| BUG-003 | `MqttLink`: registration races paho's network thread; a raise inside `on_message` ends the thread while the link still reports connected | P1 | **closed 2026-09-10 (Phase 16 Group 3, `c0224e4`: registration under the thread lock, tables before filters, `_open` re-checks, faults counted; proven by a recording lock, a refusing filter set, an injected late registration, a fault in `on_message` and one in `on_subscribe`)** | 16 | `sensor()/witness()/actuator()` mutate `_sensor_filters`/`_witness_filters`/`_ack_filters` (and `_first`/`_queues`) without `_lock`, while `_on_message` iterates them under the lock on paho's thread: `RuntimeError: Set changed size during iteration`, or a `KeyError` on `_first` (set after the filter is added), inside the callback. paho 2.1 re-raises callback exceptions by default (`suppress_exceptions=False`); `loop_forever` returns and the thread dies with `is_connected()` still true — deaf and mute. Same root: a device registered while `_open` is between its subscribe loop and `self._client = client` is never subscribed. Fix: mutate under `_lock`; set `_first`/`_queues` before adding the filter; in `_open` subscribe from a snapshot and re-check after assignment. Design F2. |
| BUG-004 | Lease, `seq` and held children reset on every resume | P0 | open (found 2026-09-10) | 6 → 7 | `run` and `resume` both build a fresh `Session`, `LeaseMeter` and `Emitter` (`runtime/loop.py` `_stream`); `RunState` holds handles, observations and iterations only. **Reproduced:** five sequential steps under a three-step lease with an Ask on step 3 — all five ran, `Ended.steps_taken` = 3, `seq` ran 0–6 twice for one run id. A parent parked while holding a child resumes with `children.held == ()` and the child is orphaned. The refusal note's "the bound has to be structural" rests on this bound holding. Fix: meter, seq and held handles ride in graph state under D19; contract minor bump. |
| BUG-005 | The assistant's tool calls are dropped from the transcript | P0 | open (found 2026-09-10) | 0 → 1 | `kernel/ports.py` `Message` has `role`, `content`, `tool_call_id` and nothing for an assistant's calls; `adapters/agent/component.py:148` appends the text and discards the calls; `adapters/langchain/model.py:141` emits a bare `AIMessage` followed by `ToolMessage`s. OpenAI and Anthropic reject a tool result with no preceding call, and even a lenient provider never shows the model which tool it called with what arguments. Every agent test uses `ScriptedModel`; `test_live.py` is single-shot; the only multi-turn wire evidence is a lenient router (`examples/real.py`). Fix: kernel minor bump adding `tool_calls` to the assistant message, carried by the LangChain adapter; one live multi-turn test behind `-m live`. |
| BUG-006 | Resume over the wire always raises; `serve` has no trust boundary | P0 | open (found 2026-09-10) | 9 | `wire/sides.py` `_drive` builds `RunOptions` with no checkpointer, so every `resume` is `ValueError: resume needs options.checkpointer` and an Ask parked over the wire is unrecoverable; no test covers it. `wire/serve.py` `open_session` issues a session on a bare GET with no credential — the run token `wire.md` lists under *rules already fixed* is unbuilt (phase-9 tasks say so). `_initialize` defaults an omitted version to a match and is never required before `_run`. No callback has a timeout, so a host that hangs in `judge` or `complete` stalls the run past its wall ceiling. `wire.md` says HTTP/2; uvicorn serves HTTP/1.1. Fix: a checkpointer per session or drop the method until it has one; loopback-only until the token lands, and say so; require `initialize`. |
| BUG-007 | mypy strict silently skips `wire`, `contained`, `derivation`; nine errors in `wire` today | P0 | open (found 2026-09-10) | 9 → 12 | `pyproject.toml` `[tool.mypy] mypy_path` omits the three packages; each ships `py.typed`, so mypy resolves them through the editable install's `.pth` entries and silences them as site-packages. "Success: 112 files" is the kernel plus whatever the tests import. **Reproduced:** `MYPYPATH=packages/wire/src:packages/adapters/contained/src:packages/adapters/derivation/src uv run mypy --no-incremental` → 9 errors in `wire/remote.py:62,136` (arg-type), `wire/context.py:95` (no-any-return), `wire/sides.py:233` (var-annotated, arg-type), `wire/sides.py:75-77,249` (unused-ignore). Fix: add the paths; fix the nine. |
| BUG-008 | Workspace escape via hard link; a NUL byte in a path raises out of `invoke` | P1 | open (found 2026-09-10) | 3 | `adapters/workspace/components.py:72` resolves symlinks, but a hard link *is* the outside inode: `os.link(outside, root/"hl.txt")` let `read_file` return the outside content and `write_file` overwrite it (reproduced). With the subprocess sandbox (`run_shell: ln ~/.zshrc x`) this is a two-component escape with no rule violated. `{"path": "a\0b"}` raises `ValueError` past the `except` at :124-128 — a contract breach. TOCTOU between `_resolve` and `write_text` (:145). `adapters.md` says `write_file` is `contained=True`; the code says `False` (:35). Fix: `lstat` and refuse `st_nlink > 1`, or open `O_NOFOLLOW` and compare `st_dev/st_ino`; catch `ValueError`. |
| BUG-009 | The subprocess leash keeps `HOME`, sets no rlimits, and orphans process trees | P1 | open (found 2026-09-10) | 3 → 11 | `runtime/leash.py:20` keeps `HOME` in `KEPT_ENV`, so a model-written script reads `~/.ssh` or removes the home directory while the profile declares `writes: {workspace}`; `:51` `kill()`s the direct child without `start_new_session` — a backgrounded `sleep` survived the step (reproduced). No memory or CPU limit; `adapters.md:166` documents a `memory_mb` that does not exist. `communicate()` buffers all output before the cap (:49,:54), so `output_limit` bounds the observation, not RAM. The contained proof (`adapters/contained/backends.py:60-96`) is a string match on `dmesg`/DMI: a five-line fake `runsc` on `PATH` passed it, and a harness host that is itself sandboxed (Cloud Run, Fly) satisfies it falsely. Fix: new session + `killpg`; drop `HOME`; rlimits; a capability test for containment or a declaration with a warning. |
| BUG-010 | `Await` and resume invoke the component twice; the re-run judgement can override the human's answer; two Asks in one `FanOut` cannot be resumed | P1 | open (found 2026-09-10) | 6 → 7 | `runtime/step.py:78-149`: LangGraph re-runs the node from the top, so `judge`, `Invoked` and `port.invoke` all precede `interrupt()` on the resume path — the component is invoked twice and `Invoked` appears twice (the code's own comments say so); only device actuators carry an idempotency key; a component that spawns re-spawns. A policy answering `Refuse` on the re-run overrides the host's `Allow`; one answering `Allow` skips the interrupt and drops the answer silently. `resume()` takes a scalar (`loop.py`) and the runtime never maps `run_id:step` to LangGraph's interrupt id, so two Asks in one superstep end `failed` with LangGraph's message. `test_await.py:64` asserts one `Observed`, not one `Invoked`. Fix: judge before the interrupt only, invoke after it; an idempotency key on every irreversible invocation; a targeted resume. |
| BUG-011 | The ACP purse double-charges cumulative usage per step; `stop()` has no kill fallback | P1 | open (found 2026-09-10) | 4 | `adapters/acp/agent.py:184-192` returns *session-cumulative* tokens on each invoke and `session.py` adds per step: two 100-token prompts charge 100 then 200. A sub-cent cost reports `cost_cents: 0` — known-zero, not unknown — against `client.py:82-88`. `stop()` (`agent.py:113-115`) has no timeout or kill, so a child that ignores SIGTERM wedges the step. Fix: charge the delta; `None` below a cent; SIGKILL after a deadline. |
| BUG-012 | `Pattern.ceiling` never read; the skill check never called; `compose` results never reach the model; usage lost when the model raises | P1 | open (found 2026-09-10) | 8 | `pattern.py:41` is parsed at `loader.py:69` and read nowhere in `component.py` — a team writing `[ceiling]` gets nothing, against `adapters.md:43`. `skills.missing_for` is exported and has no caller outside tests, against `skills.py:6-7` ("before the first turn"). `carry_out` (`component.py:334-338`) maps results by `call.id` and skips `compose`, so an authored plan's step results are dropped although `meta.py:52` promises them; four of six shipped patterns are built on `compose`. `work()` checks `max_steps` only (:138); `ports.model.complete` (:141) is unwrapped, so a provider exception becomes `Failed` and every turn's usage is never charged. `modes/check.py:48-71` `widens()` compares ceilings against the constitution's always-rules only and drops every ask line. |
| BUG-013 | Derivation: the evaluator raises out of `invoke`; the fingerprint is not canonical; floats enter | P2 | open (found 2026-09-10) | 12 | `adapters/derivation/component.py:89` calls `evaluate` outside its `try`: `{"lit":"Infinity"}`, `"1e22"`, `mul 1e15·1e15` (quantize under `prec=34`, `values.py:49-50`) and `cmp` over a NaN cell all raise `InvalidOperation` past the component. `"1"`, `"1.0"`, `"1E+1"` hash differently (`ground.py:222` `str()`s the raw Decimal); NFC vs NFD differ; `_table_from` (:119-124) passes cell types through unchecked, so `1` vs `"1"` differ and a `sum` over a float cell returned `22.5` (reproduced). Units are string equality with no cancellation. |
| BUG-014 | `FileSink`: a short write leaves a torn line silently; a torn tail plus a later append makes the file unreadable | P2 | open (found 2026-09-10) | 14 | `adapters/basic/sinks.py:50` ignores `os.write`'s return, so a short write does not raise. A torn tail followed by a later append glues two records; `proposals_in` then raises `p.jsonl:2 is not a proposal` and the rest is unreadable — the constructor never checks the tail (reproduced). `write`/`fsync` block the event loop inside `async def`; no directory fsync after `O_CREAT`; no context manager. |

## Features

| ID | Title | Priority | Status | Phase | Detail |
|----|-------|----------|--------|-------|--------|
| _(none)_ | | | | | |

## Tech Debt

| ID | Title | Priority | Status | Phase | Detail |
|----|-------|----------|--------|-------|--------|
| TD-001 | Observations ride in graph state as our own classes | P2 | **closed 2026-09-10 (D19)** | 7 → 9 | LangGraph 1.2 warns on deserializing `shadow_hdk.kernel.observations.*` from a checkpoint and says a future version will block it. **Measured 2026-09-10:** `LANGGRAPH_STRICT_MSGPACK=true uv run pytest -q` is green over 353 tests, so nothing breaks today. Two remedies: a host builds its checkpointer's serializer with `allowed_msgpack_modules` naming our module — which pushes our internals into every host's setup — or `RunState.observations` holds plain JSON and the runtime loads it back, which keeps the state boundary honest. The second is the design answer; done in Phase 9 Group 0 as **D19** — a checkpoint *is* a wire, so the state holds JSON and the runtime loads at its edge. **Verified:** `LANGGRAPH_STRICT_MSGPACK=true uv run pytest -q -W default` now reports zero such lines across 406 tests. |
| TD-002 | `MqttLink`: the witness queue is unbounded | P2 | **closed 2026-09-10 (Phase 16 Group 3, `c0224e4`: `witness(keep=)`, the oldest dropped and counted, `dropped_before` on the next act)** | 16 | `_queues[filter]` is a `deque()` with no `maxlen`; a wildcard topic chattier than the agent's invocation rate grows memory for the life of the link. Proposed: a `maxlen` per witness and a dropped counter reported as data on the next `overheard` — a late reading is data, so is a lost event. Design F3. |
| TD-003 | `wire` imports `adapters.basic.SystemClock` undeclared; intra-workspace dependencies unbounded; 11 adapters ship no `py.typed` | P1 | open (found 2026-09-10) | 9 | `wire/sides.py:71` lazily imports the basic adapter; `packages/wire/pyproject.toml:9` declares kernel + runtime only, so `shadow-hdk-wire` installed alone fails at `ports()`. `tests/invariants/test_stands_alone.py` has rules for kernel, runtime and adapters and none for `wire`. Every adapter's `dependencies` is unpinned, so published wheels could pair adapters 0.8.0 with kernel 0.1.0; `tests/test_versions.py` `EXPECTED` is the only pin. Only kernel, runtime, wire, contained and derivation ship `py.typed`; downstream mypy users of the rest get `import-untyped`. |
| TD-004 | The contract suites run against 7 of 14 adapters | P2 | open (found 2026-09-10) | 8 → 16 | `tests/adapters/contract/suites.py` is subclassed by basic, modes, langchain, workspace, mcp, acp, sandbox_subprocess and the doubles. Never by agent, contained, derivation, devices (`ComponentPort`), otel (`ObserverPort`), mqtt (`ClockPort`). The suite's own docstring — "a new adapter cannot ship having satisfied only the parts its author remembered" — is unenforced for half of them. |
| TD-005 | Nothing bounds growth: the plan cache is keyed on full JSON and never evicts; emitter, wire and witness queues are unbounded; a blocking observer holds `run()` open | P2 | open (found 2026-09-10) | 0 → 9 | `runtime/compile.py:220` keys `_PLAN_CACHE` on `dump(composition)` including every literal binding and never evicts — a long-lived host with an agent re-authoring literals leaks a `Plan` per variant; D11's "structural hash" is a misnomer and `StateGraph.compile` still runs per run. `runtime/emit.py:30` queues, `wire/serve.py:85-86` session streams, `HostSide.events` (`wire/sides.py:143`) and `post_frame` bodies are unbounded; `recording/pipes.py:38-39` bounds at 64 and is the one place that does. `_stream`'s `finally` awaits `emitter.drained()` (`loop.py`), which pumps the observer, so a blocking observer never lets `run()` return. |
| TD-006 | Governance and port handling leak outside the governed step | P2 | open (found 2026-09-10) | 0 → 13 | `runtime/bindings.py:106-112` `visible()` calls `governance.judge` unwrapped — an exception there is caught at `step.py:98` as a *component* failure and the run continues (D7 inverted). `bindings.py:194-196` `propose()` emits `Proposed` *before* an unwrapped sink call, so a sink failure leaves a proposal on the record that never arrived. `registry.py:40-43` swallows component-port exceptions into `unreachable`, which nothing reads, so a vanished MCP server is invisible on the record and `_refresh`'s `PortFailure` path is dead. `step.py:62` charges the step before it is judged, so a refuse-everything policy drains the lease. Two shapes for "refused": governance emits `Refused` with no `Observed` (:83-85); a component returning `Refused` gets `Observed(posture=controlled)` and no event. `StepExecutor.invoke` is ten moves; the docstring and `runtime.md` each say seven, and different sevens. |
| TD-007 | Posture is self-declared with a `controlled` default; the lease-at-the-act and the receipt exist only in the devices adapter | P2 | open (found 2026-09-10) | 13 → 15 | `kernel/components.py` `Provenance.posture` defaults to `controlled`; `step.py:111` and `session.py:170` copy it from the registration, so D30's "must not depend on a driver's honesty" holds per observation and not per registration — a witness that forgets the field is admitted by `Controlled` for writes. `acting.exhausted()`/`grounds()`/`Acted` are called only inside `devices/components.py:116-129`; a `writes: {world}` component registered through callables, MCP or the wire acts with no exhaustion check and no receipt, and nothing in `step.py` requires one. `wire/sides.py:222` is the one direct `port.invoke` outside the step, with a caller-supplied `run_id` and no judgement. Candidate ADR: require an `Acted` for any step whose profile is irreversible, whichever port it came through. |
| TD-008 | The constitutional specs, status and roadmap each describe a different day; the ADR directory is empty | P2 | open (found 2026-09-10) | 9 → 16 | `runtime.md`: `resume` signature, `Ports.clock` default, `Handles` class, module table (no `cancel`/`children`/`replay`/`leash`/`devices`, no `Ports.trust`) and "raises if a port fails" do not match the code; `compile.py:15-17` still says nested composites are inlined. `file-structure.md` lists `patterns/`, `effect_rules/`, `sandbox_gvisor/`, `sandbox_firecracker/`, none of which exist. `adapters.md` documents `memory_mb`, `tool_filter`, `tools=None` and `write_file` `contained=True`. `wire.md` lists the run token as fixed and HTTP/2 as the transport. `status.md` says *Latest Release: None (every package 0.0.1)*, lists phases 4–9 as *Not Started* and names Phase 9 Group 1 as the next action; `roadmap.md` says nine phases done and then 0–15, and its decision list stops at D26. `specs/decisions/` holds the template only while D14–D32 live in phase overviews, so `CLAUDE.md`'s "why was X chosen" row points at an empty folder; it also points at `docs/developer-guide.md`, which does not exist. `.coverage` is tracked. `.githooks` fail open without `node`. `session.context_for` silently overwrites host-supplied `posture`/`component` attributes. `recursion_limit = max_steps × 4` can end a nested fan-out `failed` before the lease speaks. |
| TD-009 | CI has never run on any phase commit; coverage is gated on the runtime only; the live sandbox proofs run nowhere | P1 | open (found 2026-09-10) | 0 | `.github/workflows/ci.yml:3-5` triggers on push to `main`/`staging` and on pull requests only; `gh pr list --state all` → none; `gh run list` → two runs, both on `main` at founding (2026-09-09), before any phase code. `main` is 4 commits; HEAD is ~100 ahead in a strictly linear stack of 17 branches, all pushed, none merged; both `ci.yml` and `pyproject.toml` differ from `main`'s. `--cov-fail-under=90` covers `shadow_hdk.runtime` alone. `tests/adapters/contained/test_backends.py:78-99` are `live` *and* skip without `runsc`/`SHADOW_HDK_FC_LAUNCH`, so they run nowhere. The benchmark is a wall-clock gate whose docstring records two prior flakes. Fix: `push: branches: ['**']`; one PR from `phase-16-mqtt` to `main` for a single CI run over the stack; then land once — the Rule 6 landing order is for concurrent lanes and there are none. |

## Enhancements

| ID | Title | Priority | Status | Phase | Detail |
|----|-------|----------|--------|-------|--------|
| ENH-001 | A sensor over a wildcard filter cannot say which topic it read | P2 | **closed 2026-09-10 (Phase 16 Group 3, `c0224e4`: a sensor names one topic)** | 16 | `_latest` is keyed by the filter and `Reading` carries no topic, so `plant/+/temp` collapses N thermometers into one *latest* with no name. Either a sensor names one topic (as a command topic already must) or `Reading` grows a topic field — a contract change for every protocol. Design F4. |
| ENH-002 | TLS on `MqttLink` | P3 | open (found 2026-09-10) | `[~]` | The dev broker has no TLS listener, so it cannot be proven here; a `tls: ssl.SSLContext \| None` option on `_open` lands `[~]` naming a TLS broker as what settles it. Credentials stay R9's. Design F9. |
| ENH-003 | A protocol-adapter contract suite over the device contract | P2 | open (found 2026-09-10) | before OPC-UA / ROS 2 | The environment-varied claims in `tests/adapters/mqtt/test_environment.py` (not there, refuses, leaves and returns with nothing sent late) and the role claims in `test_mqtt.py` are what OPC-UA and ROS 2 must prove too. Parametrise them over a device factory plus an environment handle the way `tests/adapters/contract` does for ports, so each `[~]` row is a fixture rather than a rewrite. Design §7. |
