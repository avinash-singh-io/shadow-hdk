---
type: Changelog
---

# Changelog — 2026-09

> Append-only; one line per change, newest last.

- 2026-09-10 — phase-16-mqtt: system-design review of the MQTT adapter as built (`specs/phases/phase-16-mqtt/design.md`); BUG-002, BUG-003, TD-002, ENH-001, ENH-002, ENH-003 filed from its findings
- 2026-09-10 — phase-16-mqtt Group 3: BUG-002, BUG-003, TD-002, ENH-001 fixed (`c0224e4`); the ineffective stress test replaced by direct claims; 691 tests
- 2026-09-10 — **architecture review of the whole harness** at `4331178`: eleven bugs (BUG-004…014) and seven tech-debt items (TD-003…009) filed. Four P0: the lease and `seq` reset on every resume (reproduced), the assistant's tool calls never reach the model, `resume` over the wire always raises while `serve` has no trust boundary, and mypy strict silently skips three packages with nine live errors in `wire`. CI has never run on a phase commit. The architecture itself holds — lattice, one governed step, layering, refusal — so the recommendation is to fix the gate, then land the linear stack once rather than sixteen times.
- 2026-09-10 — BUG-001 fixed as a quick-task (`67e2490`): a tool named like an agent meta-tool is refused naming both sides, not shadowed; 695 tests
- 2026-09-10 — phase-16-mqtt: five Archify diagrams of the MQTT adapter (architecture, sequence, lifecycle, dataflow, workflow) in `specs/phases/phase-16-mqtt/diagrams/`, sources and delivered HTML
- 2026-09-10 — a full-codebase audit landed in the backlog: BUG-004..BUG-014, TD-003..TD-009. BUG-007 fixed (`8a9e566`): mypy now checks all 116 files including the wire; every mypy-0 from Phase 9 to Phase 16 had excluded it
- 2026-09-10 — phase-17 group 1 (`e1d6ef3`): BUG-004 fixed — what a run has spent survives a park (D33); contract 0.9.0; BUG-015 filed for the held-children half; 711 tests
- 2026-09-10 — phase-17 group 2 (`b36dada`): BUG-005 fixed — an assistant message carries the calls it made; contract 0.10.0; 717 tests
- 2026-09-10 — phase-17 group 3 (`1f44578`): BUG-006 fixed — a parked run resumes over the wire (D34); `initialize` required; callbacks time out; `serve` loopback-only without a token; `wire.md` corrected. **Phase 17 complete: all four P0s closed.** 729 tests
- 2026-09-10 — phase-18 group 1 (`7fe2b0a`): BUG-008 closed (a hard link is not confined) and BUG-009's leash half (D35: a step owns its process tree; no `HOME`; `memory_mb` real on Linux). The containment proof stays open. 752 tests
- 2026-09-10 — phase-18 group 2 (`69bf520`): BUG-009 closed whole — D36, containment is proven by what is denied; a fake `runsc` that announced gVisor is now refused. 764 tests
- 2026-09-10 — phase-18 group 3 (`f378d9c`): BUG-015 closed — D37, a parent that parks comes back holding its children; contract 0.11.0. 771 tests
- 2026-09-10 — phase-18 group 4 (`103de41`): BUG-010 closed — D38, a parked step resumes where it parked; an `Await` no longer invokes twice and a re-run judgement can no longer overturn the human's answer, which must now be a `Judgement`. **Contract 0.11.0 → 0.12.0.** The audit's row was wrong on the fan-out case and is corrected in the backlog; five tests that encoded the old behaviour were rewritten. 779 tests
- 2026-09-10 — five system-level Archify diagrams of the harness itself in `specs/architecture/diagrams/` (architecture, sequence, governed-step workflow, run lifecycle, plan-to-record dataflow), each passing the showcase gate; linked from the architecture reading order
- 2026-09-10 — `specs/architecture/diagrams/reading-guide.md`: every component named and explained, a box-by-box walk of each of the five diagrams, the six directions data can flow (and which two are loops), and the five ways to consume the harness
- 2026-09-10 — the explanation moved **into** the diagrams: each of the six now carries a subtitle, five numbered chapters that walk it box by box, and cards naming every box; the architecture page links each box to its source file at a pinned revision. `consuming.architecture` added for the three ways in; the standalone reading guide deleted as redundant
- 2026-09-10 — phase-18 group 5: **BUG-011** (`64e74e8`) — a step is charged what that step spent, cents are the difference of the rounded totals so sub-cent money is not lost, and a child that ignores `SIGTERM` is killed with the leash's own rule (D35), moved below both. **BUG-012** (`2ed4514`) — five promises with no caller: a `compose` call left unanswered (the dangling call every provider rejects, and four of six patterns are built on it), spend lost when a provider raised, `Pattern.ceiling` and `skills.missing_for` never read, `widens()` blind to ask lines. **TD-003** (`c5c1dc6`) — the wire imports no adapter, all seventeen distributions pin `==0.12.0`, twelve `py.typed` markers, verified from a built wheel. **TD-009** (`8a78177`) — CI now triggers on every push, and the pull request is prepared for the owner and not opened. **Phase 18 complete: every P0 and P1 closed.** 837 tests; 43 mutations across the group
- 2026-09-10 — **the first CI run in this repository's history** (`5cd57b4`) went red (`5036ef8`): lint, format and types passed on Linux, coverage came back 96.77%, and the test that skips here for want of `prlimit` ran on Linux and passed. The benchmark failed because the gating step traces the very package it measures — 179.7 ms clean against 425.3 ms under coverage, on a 300 ms assertion; it is measured uninstrumented now. **BUG-016 filed**: with the numbers read properly the runtime costs 1.36 ms/step against D11's ≤ 1 ms, and 2.4× the figure recorded in the file — a drift the 3× assertion was never going to catch
- 2026-09-10 — phase-19 group 1 (`3ffbde2`, `e85715c`): **BUG-016 closed** — a contract's `TypeAdapter` is built once per type, not once per call. 57% of the runtime's per-step overhead was Pydantic rebuilding the same schema; 1.36 → **0.594 ms/step**, inside D11's ≤ 1 ms. D11's 3× slack deliberately kept, because the runner is 2.1× this machine and the drift was 2.4× — a stopwatch cannot separate them, so a count of adapters guards it instead. **CI green for the first time in this repository's history.** 845 tests
- 2026-09-10 — phase-19 group 2 (`8ea82ad`, `b87de9f`): **BUG-013** — the derivation engine is total (`out_of_range` rather than a raise out of `invoke`), magnitudes are canonical so one quantity has one fingerprint, text normalises to NFC on both sides of a query, and a cell is a string or a boolean. **The audit's row corrected in three places**, and two existing tests that encoded the weaker property were rewritten. **BUG-014** — every byte written or a raise; a torn tail cut back before any append, which retires Phase 14's *a torn line can only be the last* (true within one process, false across the restart that is the only time one exists); the record written off the event loop; the directory synced once; a context manager. 871 tests; 24 mutations, 23 bite, 1 equivalent and named
- 2026-09-10 — phase-19 group 3 (`f2b1f20`): **TD-004** — the contract suites reached 7 of 14 adapters; the fix is an **invariant** that requires every port implementation to name the module contracting it, checked to really contain a contract. The scan found 29 implementations, not fourteen. **The suites found no defects**, so the net was widened by one question: inputs of the wrong shape are an observation, not an exception. **TD-005** — the plan cache bounded at 512 LRU (2000 compositions had held 2000 entries, ~6.3 MiB) and the observer queue at 4096 dropping the oldest and counting (a slow observer had handled 1 event while 49,999 queued); the caller's stream stays unbounded and argued. **Two of TD-005's three claims were stale.** 924 tests; 11 mutations, all bite
- 2026-09-10 — phase-19 group 4 (`1aeda06`, `b08988b`): **TD-006** — six claims, one cause. `RuntimeStop` was an `Exception`, so every component adapter's `except Exception` swallowed a lease that ran out, a cancellation and a port failure alike; it is a **`BaseException`** now, the argument that moved `asyncio.CancelledError` in 3.8. `visible()` wraps its judgement, `propose()` announces only what the sink took, `unreachable` is readable, and the lease is charged for work rather than for being refused. The step does **nine** moves, not the seven two documents claimed. **Contract 0.12.0 → 0.13.0.** **TD-007** plumbing closed (`posture` and `component` refused at the door) and **its policy recorded as the owner's under ADR-1**. **TD-008** in part: `specs/decisions/` maps all thirty-eight, with an invariant keeping it true. 940 tests; 9 mutations, all bite
- 2026-09-10 — phase-19 complete (`c658d2a`): **TD-008's document half** — the fix is an invariant rather than a pass, so a path a document names must exist, the adapter listing must match the tree, and the module table must match the tree. It found two dead links in `CLAUDE.md`, four wrong directories and two missing ones in `file-structure.md`, and **thirteen of twenty** modules in `runtime.md`'s table. Three of the row's own claims were already false and one does not reproduce. **Phase 19 is complete and every P0, P1 and P2 the audit filed is closed** — ten of its claims corrected on the record along the way. 946 tests; mypy over 133 files; 0.13.0
- 2026-09-10 — **MERGED AND RELEASED.** The owner approved landing the stack end to end. `staging` created from the top of the stack and `main` fast-forwarded onto it — no merge commit, because `main` was a strict ancestor 163 behind with nothing of its own. All three branches at `81d4b6a`, 165 commits, CI green. **v0.13.0 tagged and released**, the first release this repository has had: seventeen distributions all MIT, 946 tests, mypy strict over 133 files, 0.594 ms of runtime overhead per step. The release notes state the known limits rather than omitting them — the live containment proofs, OPC-UA and ROS 2, TLS, unit cancellation, and the two governance questions that belong in an ADR
- 2026-09-11 — **BUG-017**: `served_over_http` printed a `CancelledError` traceback on a clean exit, because it cancelled uvicorn mid-`serve` instead of asking it to stop and waiting. Invisible to every wire test — pytest's anyio runner absorbs an unretrieved exception — so the regression runs a listener in a subprocess under plain `asyncio.run` and reads its stderr. The first fix waited on `server.started`, which uvicorn never clears, and so was a five-second sleep that passed for the wrong reason; it waits on `serve` returning now (26.3 s → 5.8 s). `specs/status.md` also brought up to date — its phase tables still said *unmerged* and *Not Started* for work that shipped, and the release column was empty for thirteen releases. **A carried-forward number was wrong and is corrected here**: 133 was Group 1's gate, and the phase's closing records, the release notes and the prepared pull request all repeated it instead of re-measuring after four more groups of files landed. Measured at the phase-19 tree it is **146**, and 147 with this change's one new test. The group gates in `specs/phases/phase-19-the-p2s/tasks.md` are left as they were — each recorded what it actually ran. `README.md` rewritten from Phase 0 — it announced 150 tests, version 0.1.0 and *private and unlicensed* a day after the MIT release — and the documents invariant widened to cover it, which is the part that stops this recurring
- 2026-09-11 — **v0.13.1 released.** The owner approved landing `fix/BUG-017-and-the-front-door` end to end. Seventeen distributions bumped 0.13.0 → 0.13.1 and the sixteen intra-workspace pins with them, because equality is what *every package moves together* means. **A patch, not a minor**: no contract changed, so there is no *Pins* row on the board — the only behaviour that moved is `served_over_http` leaving without printing a traceback. The rest is the front door: a README that describes this tree rather than Phase 0's, held there by the documents invariant
- 2026-09-11 — **Phase 20: providers.** Bring your own key already worked; this is bring your own subscription. **D39** two seams — `ModelPort` sells inference and the caller owns the loop, `AgentPort` sells agency and owns its own; a CLI-backed `ModelPort` cannot be built honestly and the reference implementation's twenty-eight provider definitions contain no such seam. **D40** a provider is a TOML file like a pattern — `opencode` was added with no Python at all. **D41** asked, never read: no credential is opened, stored, forwarded or logged, and five statuses where `unknown` is honesty. **D42** the socket — the child is launched with our registry and its native tools refused, so its loop runs and our governance judges every effect. **D43** the loop stays theirs, named rather than left as a gap. The ACP terminal door opened on the runtime's new `HeldProcess`; invariant rule 5 keeps the selection surface free of adapters, which discover themselves through entry points. Four measured quirks, three of them wrong answers rather than errors, each now a field with a regression test. Contract **0.14.0**; eighteen distributions; 1043 tests
- 2026-09-11 — **Phase 20 proven end to end.** A second transport, `jsonl`, and with it `Dialect`: how one CLI names things, as data. Three providers over two transports — `codex` cost a file because the transport written for Claude Code already existed, which is the property the whole design is for. **D44**: the registry is connected to, never launched, so a CLI that starts its own MCP servers starts a relay into ours. The coder example runs against a live subscription: Claude Code reasons, `WorkspaceComponents` writes and `SubprocessSandbox` runs, each as a child run judged and charged. Four obstacles on the way, all now fields rather than code paths — its own permission layer, its tool-name prefix, the driver component being on offer, and an `EffectProfile` of booleans that `mypy` would have caught had `examples/` been in the net; it is now, which also closed **ENH-004**

