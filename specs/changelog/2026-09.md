---
type: Changelog
---

# Changelog — 2026-09

- 2026-09-15 — **README refreshed for v0.30.0**: the public entry point now describes Shadow HDK
  as a progressive harness development kit, distinguishes it from a product or one fixed agent,
  explains its capability/authority/durable-record boundaries, and links readers to the consumer,
  migration, package, architecture, roadmap, and release documentation.

> Append-only; one line per change, newest last.

- 2026-09-10 — phase-16-mqtt: system-design review of the MQTT adapter as built (`specs/phases/phase-16-mqtt/design.md`); BUG-002, BUG-003, TD-002, ENH-001, ENH-002, ENH-003 filed from its findings
- 2026-09-10 — phase-16-mqtt Group 3: BUG-002, BUG-003, TD-002, ENH-001 fixed (`b46ef84`); the ineffective stress test replaced by direct claims; 691 tests
- 2026-09-10 — **architecture review of the whole harness** at `1b91815`: eleven bugs (BUG-004…014) and seven tech-debt items (TD-003…009) filed. Four P0: the lease and `seq` reset on every resume (reproduced), the assistant's tool calls never reach the model, `resume` over the wire always raises while `serve` has no trust boundary, and mypy strict silently skips three packages with nine live errors in `wire`. CI has never run on a phase commit. The architecture itself holds — lattice, one governed step, layering, refusal — so the recommendation is to fix the gate, then land the linear stack once rather than sixteen times.
- 2026-09-10 — BUG-001 fixed as a quick-task (`bee109c`): a tool named like an agent meta-tool is refused naming both sides, not shadowed; 695 tests
- 2026-09-10 — phase-16-mqtt: five Archify diagrams of the MQTT adapter (architecture, sequence, lifecycle, dataflow, workflow) in `specs/phases/phase-16-mqtt/diagrams/`, sources and delivered HTML
- 2026-09-10 — a full-codebase audit landed in the backlog: BUG-004..BUG-014, TD-003..TD-009. BUG-007 fixed (`6f45e4c`): mypy now checks all 116 files including the wire; every mypy-0 from Phase 9 to Phase 16 had excluded it
- 2026-09-10 — phase-17 group 1 (`fa55d1c`): BUG-004 fixed — what a run has spent survives a park (D33); contract 0.9.0; BUG-015 filed for the held-children half; 711 tests
- 2026-09-10 — phase-17 group 2 (`ac72a24`): BUG-005 fixed — an assistant message carries the calls it made; contract 0.10.0; 717 tests
- 2026-09-10 — phase-17 group 3 (`d81ed7f`): BUG-006 fixed — a parked run resumes over the wire (D34); `initialize` required; callbacks time out; `serve` loopback-only without a token; `wire.md` corrected. **Phase 17 complete: all four P0s closed.** 729 tests
- 2026-09-10 — phase-18 group 1 (`21ebe6a`): BUG-008 closed (a hard link is not confined) and BUG-009's leash half (D35: a step owns its process tree; no `HOME`; `memory_mb` real on Linux). The containment proof stays open. 752 tests
- 2026-09-10 — phase-18 group 2 (`27e81a2`): BUG-009 closed whole — D36, containment is proven by what is denied; a fake `runsc` that announced gVisor is now refused. 764 tests
- 2026-09-10 — phase-18 group 3 (`e9c631b`): BUG-015 closed — D37, a parent that parks comes back holding its children; contract 0.11.0. 771 tests
- 2026-09-10 — phase-18 group 4 (`53747e5`): BUG-010 closed — D38, a parked step resumes where it parked; an `Await` no longer invokes twice and a re-run judgement can no longer overturn the human's answer, which must now be a `Judgement`. **Contract 0.11.0 → 0.12.0.** The audit's row was wrong on the fan-out case and is corrected in the backlog; five tests that encoded the old behaviour were rewritten. 779 tests
- 2026-09-10 — five system-level Archify diagrams of the harness itself in `specs/architecture/diagrams/` (architecture, sequence, governed-step workflow, run lifecycle, plan-to-record dataflow), each passing the showcase gate; linked from the architecture reading order
- 2026-09-10 — `specs/architecture/diagrams/reading-guide.md`: every component named and explained, a box-by-box walk of each of the five diagrams, the six directions data can flow (and which two are loops), and the five ways to consume the harness
- 2026-09-10 — the explanation moved **into** the diagrams: each of the six now carries a subtitle, five numbered chapters that walk it box by box, and cards naming every box; the architecture page links each box to its source file at a pinned revision. `consuming.architecture` added for the three ways in; the standalone reading guide deleted as redundant
- 2026-09-10 — phase-18 group 5: **BUG-011** (`176f619`) — a step is charged what that step spent, cents are the difference of the rounded totals so sub-cent money is not lost, and a child that ignores `SIGTERM` is killed with the leash's own rule (D35), moved below both. **BUG-012** (`85f0f14`) — five promises with no caller: a `compose` call left unanswered (the dangling call every provider rejects, and four of six patterns are built on it), spend lost when a provider raised, `Pattern.ceiling` and `skills.missing_for` never read, `widens()` blind to ask lines. **TD-003** (`f385fc5`) — the wire imports no adapter, all seventeen distributions pin `==0.12.0`, twelve `py.typed` markers, verified from a built wheel. **TD-009** (`fde1a35`) — CI now triggers on every push, and the pull request is prepared for the owner and not opened. **Phase 18 complete: every P0 and P1 closed.** 837 tests; 43 mutations across the group
- 2026-09-10 — **the first CI run in this repository's history** (`fd0de9c`) went red (`51cc222`): lint, format and types passed on Linux, coverage came back 96.77%, and the test that skips here for want of `prlimit` ran on Linux and passed. The benchmark failed because the gating step traces the very package it measures — 179.7 ms clean against 425.3 ms under coverage, on a 300 ms assertion; it is measured uninstrumented now. **BUG-016 filed**: with the numbers read properly the runtime costs 1.36 ms/step against D11's ≤ 1 ms, and 2.4× the figure recorded in the file — a drift the 3× assertion was never going to catch
- 2026-09-10 — phase-19 group 1 (`5bcc0fd`, `fedae7a`): **BUG-016 closed** — a contract's `TypeAdapter` is built once per type, not once per call. 57% of the runtime's per-step overhead was Pydantic rebuilding the same schema; 1.36 → **0.594 ms/step**, inside D11's ≤ 1 ms. D11's 3× slack deliberately kept, because the runner is 2.1× this machine and the drift was 2.4× — a stopwatch cannot separate them, so a count of adapters guards it instead. **CI green for the first time in this repository's history.** 845 tests
- 2026-09-10 — phase-19 group 2 (`6c94335`, `8a3992c`): **BUG-013** — the derivation engine is total (`out_of_range` rather than a raise out of `invoke`), magnitudes are canonical so one quantity has one fingerprint, text normalises to NFC on both sides of a query, and a cell is a string or a boolean. **The audit's row corrected in three places**, and two existing tests that encoded the weaker property were rewritten. **BUG-014** — every byte written or a raise; a torn tail cut back before any append, which retires Phase 14's *a torn line can only be the last* (true within one process, false across the restart that is the only time one exists); the record written off the event loop; the directory synced once; a context manager. 871 tests; 24 mutations, 23 bite, 1 equivalent and named
- 2026-09-10 — phase-19 group 3 (`a670f30`): **TD-004** — the contract suites reached 7 of 14 adapters; the fix is an **invariant** that requires every port implementation to name the module contracting it, checked to really contain a contract. The scan found 29 implementations, not fourteen. **The suites found no defects**, so the net was widened by one question: inputs of the wrong shape are an observation, not an exception. **TD-005** — the plan cache bounded at 512 LRU (2000 compositions had held 2000 entries, ~6.3 MiB) and the observer queue at 4096 dropping the oldest and counting (a slow observer had handled 1 event while 49,999 queued); the caller's stream stays unbounded and argued. **Two of TD-005's three claims were stale.** 924 tests; 11 mutations, all bite
- 2026-09-10 — phase-19 group 4 (`4371b58`, `b1b9308`): **TD-006** — six claims, one cause. `RuntimeStop` was an `Exception`, so every component adapter's `except Exception` swallowed a lease that ran out, a cancellation and a port failure alike; it is a **`BaseException`** now, the argument that moved `asyncio.CancelledError` in 3.8. `visible()` wraps its judgement, `propose()` announces only what the sink took, `unreachable` is readable, and the lease is charged for work rather than for being refused. The step does **nine** moves, not the seven two documents claimed. **Contract 0.12.0 → 0.13.0.** **TD-007** plumbing closed (`posture` and `component` refused at the door) and **its policy recorded as the owner's under ADR-1**. **TD-008** in part: `specs/decisions/` maps all thirty-eight, with an invariant keeping it true. 940 tests; 9 mutations, all bite
- 2026-09-10 — phase-19 complete (`3bcc02b`): **TD-008's document half** — the fix is an invariant rather than a pass, so a path a document names must exist, the adapter listing must match the tree, and the module table must match the tree. It found two dead links in `CLAUDE.md`, four wrong directories and two missing ones in `file-structure.md`, and **thirteen of twenty** modules in `runtime.md`'s table. Three of the row's own claims were already false and one does not reproduce. **Phase 19 is complete and every P0, P1 and P2 the audit filed is closed** — ten of its claims corrected on the record along the way. 946 tests; mypy over 133 files; 0.13.0
- 2026-09-10 — **MERGED AND RELEASED.** The owner approved landing the stack end to end. `staging` created from the top of the stack and `main` fast-forwarded onto it — no merge commit, because `main` was a strict ancestor 163 behind with nothing of its own. All three branches at `c0bc9e7`, 165 commits, CI green. **v0.13.0 tagged and released**, the first release this repository has had: seventeen distributions all MIT, 946 tests, mypy strict over 133 files, 0.594 ms of runtime overhead per step. The release notes state the known limits rather than omitting them — the live containment proofs, OPC-UA and ROS 2, TLS, unit cancellation, and the two governance questions that belong in an ADR
- 2026-09-11 — **BUG-017**: `served_over_http` printed a `CancelledError` traceback on a clean exit, because it cancelled uvicorn mid-`serve` instead of asking it to stop and waiting. Invisible to every wire test — pytest's anyio runner absorbs an unretrieved exception — so the regression runs a listener in a subprocess under plain `asyncio.run` and reads its stderr. The first fix waited on `server.started`, which uvicorn never clears, and so was a five-second sleep that passed for the wrong reason; it waits on `serve` returning now (26.3 s → 5.8 s). `specs/status.md` also brought up to date — its phase tables still said *unmerged* and *Not Started* for work that shipped, and the release column was empty for thirteen releases. **A carried-forward number was wrong and is corrected here**: 133 was Group 1's gate, and the phase's closing records, the release notes and the prepared pull request all repeated it instead of re-measuring after four more groups of files landed. Measured at the phase-19 tree it is **146**, and 147 with this change's one new test. The group gates in `specs/phases/phase-19-the-p2s/tasks.md` are left as they were — each recorded what it actually ran. `README.md` rewritten from Phase 0 — it announced 150 tests, version 0.1.0 and *private and unlicensed* a day after the MIT release — and the documents invariant widened to cover it, which is the part that stops this recurring
- 2026-09-11 — **v0.13.1 released.** The owner approved landing `fix/BUG-017-and-the-front-door` end to end. Seventeen distributions bumped 0.13.0 → 0.13.1 and the sixteen intra-workspace pins with them, because equality is what *every package moves together* means. **A patch, not a minor**: no contract changed, so there is no *Pins* row on the board — the only behaviour that moved is `served_over_http` leaving without printing a traceback. The rest is the front door: a README that describes this tree rather than Phase 0's, held there by the documents invariant
- 2026-09-11 — **Phase 20: providers.** Bring your own key already worked; this is bring your own subscription. **D39** two seams — `ModelPort` sells inference and the caller owns the loop, `AgentPort` sells agency and owns its own; a CLI-backed `ModelPort` cannot be built honestly and the reference implementation's twenty-eight provider definitions contain no such seam. **D40** a provider is a TOML file like a pattern — `opencode` was added with no Python at all. **D41** asked, never read: no credential is opened, stored, forwarded or logged, and five statuses where `unknown` is honesty. **D42** the socket — the child is launched with our registry and its native tools refused, so its loop runs and our governance judges every effect. **D43** the loop stays theirs, named rather than left as a gap. The ACP terminal door opened on the runtime's new `HeldProcess`; invariant rule 5 keeps the selection surface free of adapters, which discover themselves through entry points. Four measured quirks, three of them wrong answers rather than errors, each now a field with a regression test. Contract **0.14.0**; eighteen distributions; 1043 tests
- 2026-09-11 — **Phase 20 proven end to end.** A second transport, `jsonl`, and with it `Dialect`: how one CLI names things, as data. Three providers over two transports — `codex` cost a file because the transport written for Claude Code already existed, which is the property the whole design is for. **D44**: the registry is connected to, never launched, so a CLI that starts its own MCP servers starts a relay into ours. The coder example runs against a live subscription: Claude Code reasons, `WorkspaceComponents` writes and `SubprocessSandbox` runs, each as a child run judged and charged. Four obstacles on the way, all now fields rather than code paths — its own permission layer, its tool-name prefix, the driver component being on offer, and an `EffectProfile` of booleans that `mypy` would have caught had `examples/` been in the net; it is now, which also closed **ENH-004**
- 2026-09-11 — **BUG-018 (P0), found by running the example.** The subprocess sandbox and the ACP `execute` kind declared `reads`/`writes` as `{workspace}` however uncontained they were, so an agent asked for a landing page read `specs/status.md`, the parent directory, unrelated projects and `/Applications`, and nothing refused it. The same function got `reaches` right and said why in its own docstring, so the rule was written down and applied to one field of three — twice, in two packages that cannot import each other. Both honest now and both tested by *doing it*. A host that permits running code has to write `everything`, which is what it grants; the example says so in a banner and `--confined` shows the refusal. Also fixed: a mode forbidding `reaches` refused the conversation itself and the example deadlocked instead of reporting it
- 2026-09-11 — **Phase 21: the visible agent.** **D45** `Reasoned`, the twelfth event kind — what the model thought, on the record ahead of what it did; where thinking lives is a field on the dialect, not a branch; three transports carry it; telemetry carries length, never text. **D46** the stream folds into steps once, as a pure function, and crosses the wire already folded — one `Fold`, both sides. **D47** a large result is held by the agent and never written by the runtime (the plan said a file; the second principle said no). Deferred schemas were already built; one lie in them fixed and the how-to-ask sentence said once, not two hundred times. Measured against a real subscription: Claude Code redacts reasoning in print mode; `--thinking-display summarized` is the flag and a summary is what arrives. Found for Phase 23: the agent adapter cannot run over the wire — two of three crossed-context gaps closed here. Contract **0.15.0**; nineteen distributions
- 2026-09-11 — **Phase 22: the environment.** **D48** one environment with a mode — read-only, workspace-write, full — and one derivation of every operation's profile from what is *true* of the environment; a mode the isolation cannot make true is refused, never widened. **D49** local execution confined by the OS sandbox (seatbelt, bubblewrap), proven before the environment exists; measured live on a subscription: a write to `$HOME` from an agent's script is `EPERM`, nothing escaped. **D50** an isolated environment behind a Box seam proven by two denials; OpenSandbox consumed as the first backend; the gVisor and Firecracker wrappers deleted. `workspace`, `sandbox_subprocess`, `contained` deleted with every claim worth keeping carried over (fifty tests); twenty-one stale phase frontmatters corrected and held by a rule. Seventeen distributions at **0.16.0**; 1,094 tests

- 2026-09-11 — **Phase 23: a host, in-process and in any language.** **D51** the agent spawns the plans its model authors through the runtime, so a sub-agent runs where the record is whichever side of the wire the agent is on; the pattern's ceiling crosses as data and is applied there (`Narrowed`); every `RunContext` method crosses or is named with a reason, and every event kind — `Reasoned` and the `Step` projection included — is in the published schemas, both held by invariants. **D52** the registry socket is authenticated: a per-serve token from `secrets`, in the relay's environment, first line before MCP, constant-time, refusals counted on the holder and never logged (D44's debt paid; seven mutants killed, two tests rewritten from the catalogue). **D53** a provider's child dies with the process that held it — `hold`/`let_go` at every spawn site, `atexit` plus a chained `SIGTERM`/`SIGHUP` handler installed only where the host has none, exit status preserved; BUG-019 reproduced under four endings before the fix and proven gone in a subprocess under five. **A host example** (`examples/host/`): its own policy over effects, ledger, sqlite checkpointer and view handed in; a brief parks on a question, persists, and resumes from the next call; three brains — scripted, by key, by subscription — one run; measured live on Claude Code, one turn. `run_steps(nested=True)` yields every step as it closes with its `parent`, because a host waiting for the orchestrator's step rendered nothing for the whole run. The live proofs run on demand (`live.yml`, `workflow_dispatch`, `-m live -rs`) and never on a push. Codex 0.154.0 installed into the scratch directory (never globally) and read off signed out; `codex.toml` says which fields are measured and which stay transcribed; `detect()` now honours `CODEX_BIN`/`CLAUDE_BIN`, which it dropped. **BUG-020 filed (P1):** an `Ask` inside an agent's tool call parks the child and tells the model the step did not run — the host is never in the loop. Contract change (`Step.parent`, `serve_over_socket` yields `(port, token)`, `run_steps(nested=)`): **0.17.0**, every package, a *Pins* row. 1,127 tests; mypy strict over 194 files.
- 2026-09-11 — **Phase 24: the skill registry.** **D54** a skill is a registry entry — `description` (the line the model chooses by) and `source` (shipped · file · minted · kept · the host's own); `SkillRegistry` a union of sources, later shadowing earlier on the record; four shipped skills, none about code. **D55** the registry is a **component**: `use_skill` is pure, carries the names and lines on its own description rebuilt every catalogue, runs D17's check against `visible()` on choice, and the choice is a step on the record — reachable in-process, over the wire, and by a CLI through the registry socket; the meta-tool form was built first, eight mutants killed, and replaced, because two mechanisms for one act is what principle 5 is against. **D56** `mint_skill` (offered where the host allows, `writes: {record}` so a mode hides it by effect) adds to the registry's `minted` source and proposes `kind="skill"` through the sink; `kept_from` is the host's half; the runtime keeps nothing. Measured live through the coder: Claude Code chose `look-before-you-change` over the socket and followed it — read, smallest change, re-read. The host example lists, chooses, mints and keeps (its ledger is a source). Contract change (`Skill` fields, `SkillComponents`, `AgentComponent` unchanged): **0.18.0**, every package, a *Pins* row. 1,153 tests; mypy strict over 198 files.
- 2026-09-12 — **Hardening to Phase 24** (`harden-to-24`, no new phase). **BUG-020 closed (D57)**: a component may answer `Asked` and the run parks on it as if governance had; on resume it finds the answer and what it kept, both riding the interrupt payload the checkpointer holds — the agent adapter uses it, so an Ask inside an agent's tool call reaches the host, allowed runs, refused is told, nested agents included. **BUG-021 found and closed (D58)**: a subscription CLI's tool call the policy asks about is answered *live* while the CLI waits — `Questions`, a handle the host keeps like `Cancellation`; `ctx.ask()`; the RecordingServer runs the call as a held child and sends it the answer; measured with Claude Code. **BUG-022 found and closed**: a child asking for the parent's remaining wall-clock across a second boundary was refused; a reservation clamps the clock. **Codex measured signed in** on the owner's login: sub-typed items, usage, the override spelling, the pre-permit (`default_tools_approval_mode`), the failure shape; the end-to-end relay proof is a live test that skipped on the free tier's quota; ENH-005 filed (no strict MCP mode). **`examples/studio/`**: a host with a page — conversation, the record as steps with Allow/Refuse on a live question, the workspace's files — driven in the browser on the subscription. The coder's `full` mode asks before every write; `--provider=`. BUG-019's row closed. Contract change: **0.19.0**. 1,185 tests.
- 2026-09-12 — **The studio, driven like a person would** (`studio-scenarios`, no new phase). The page rebuilt the way agent UIs converge: the trace **inline in the conversation** as ordered parts — reasoning, tool calls with results folded under, question cards with Allow/Refuse where they were asked, markdown answers — and the **environment** beside it (the root's files, changed ones marked). Five real tasks on the owner's subscription: a `wc` CLI with 24 tests; a **playable Snake game** (played in the browser; a write to `/tmp` denied by seatbelt and recovered from); a refactor that found a `>`/`>=` drift, asked before deciding, proved the new test fails on the old code and committed; a CSV report with `pip install` **refused by the person** and the stdlib used instead; a read-only session whose write was denied at two doors and reported honestly. **Seven bugs found live, reproduced, closed**: BUG-023 `/dev/null` unwritable in a confined mode (git could not run); BUG-024 a CLI's parallel tool calls born exhausted (one call reserved the whole budget; a costless step is now never refused for money it cannot spend); BUG-025 a `Failed` for an exception group named nothing; BUG-026 a question named nothing (**D59**: `Asked` carries `component` and `inputs`, withdrawn questions announced); BUG-027 a result written to a connection the CLI had torn down ended the conversation (**D60**: a connection's death is contained); BUG-028 the relay's thirty-second socket timeout killed it while a person decided; BUG-029 read-only refused the conversation itself. ENH-006/007/008 filed. Contract change: **0.20.0**. 1,201 tests; mypy strict over 213 files.
- 2026-09-12 — **Phase 25 opened: the host's controls** (`phase-25-the-hosts-controls`). **Group 1 — the record speaks the industry's words (D61)**: `reasoning`, `usage`, `approval_requested`, and a thirteenth kind `input_requested`; `ApprovalRequest`/`InputRequest` observations; the projection is `Item` (`runtime.items`); the handle is `Approvals` answered `Approve`/`Deny`/`ApproveAndAddRule`; the wire's `item` notification. Contract change: **0.21.0** across every package. 1,206 tests.
- 2026-09-12 — **Phase 25 group 2 — `Thread` (D62)**: the container every product has, in the runtime; a turn is a run, the agent's tool calls child runs under it; the registry offered for the thread's lifetime under the host's name (`SocketOffer`, `InProcessOffer`; the routing moved into `runtime.offer`); `ThreadStore` port with in-memory and sqlite stores under one contract; fork and rollback honest about the provider's transcript; the coder's `session` module gone; `providers.ready()`. Measured live: one turn, one run, two child runs, 26¢. 1,231 tests.
- 2026-09-12 — **Phase 25 group 3 — activity (D63)**: `Activity` beside the record — thinking and text deltas, a running command's output, forwarded from children, bounded, never checkpointed; `ActivityObserver`; `Delta` rows on the `Dialect` measured on Claude Code; the leash's `on_output`; `steer`/`interrupt` on the session and the thread; `context.activity` crosses the wire. Measured live: 38 thinking + 36 text deltas streamed into the studio, a command's lines arriving one by one. Three bugs found by it: a thought recorded twice, `QueueFull` at close, items not kept. 1,247 tests.
- 2026-09-12 — **Phase 25 group 4 — modes (D64)**: `Behaviour` in the kernel; `ModeSpec` = policy + behaviour + presentation; `ModeRegistry` with the three shipped defaults named as the environment's modes, out of the example; provider files map a behaviour to flags (`behaviour_args`; Claude Code's four measured; an unmapped field is reported, never dropped); `AgentPort.open(behaviour=)`; `Thread.set_mode`/`set_option`, reopening the provider only when the behaviour changes, `ModeChanged` on the record. 1,258 tests.
- 2026-09-12 — **Phase 25 group 5 — approvals and input (D65)**: `ActRule` in the kernel; `ActRules` registry consulted by `ModeGovernance` after it says *ask*; `RunOptions.rules` a host handle; `accept_answer` on both paths adds the rule and proposes it through the sink; the context carries the act's `inputs`; `ask_person` component + `RunContext.request_input` + `InputRequested`, crossing the wire; the studio's *Approve & don't ask again* and a text reply for input requests. Measured live: asked once, rule kept, second write silent; the agent asked "which language?" and wrote नमस्ते!. 1,279 tests.
- 2026-09-12 — **Phase 25 group 6 — the Store (D66)**: `Store` port (kernel) with `InMemoryStore` and `SqliteStore`; every registry takes it as a source — modes (`store_modes`, plus `modes_in` for files in OpenCode's shape), act rules (`store_rules`, written through), skills (`store_skills`), components (`Switched`/`store_switches`), providers (`store_providers`/`library_from`); governance reads the mode registry live; the invariant that every registry has a store source; the studio's `--store` and admin endpoints. Measured live: a mode and a rule created at runtime took effect on the next turn. 1,308 tests.
- 2026-09-12 — **Phase 25 group 7 and close**: the studio folds consecutive tool items into "Ran N commands, used M tools ›"; the duplicate final thought fixed at the source (`RunContext.recorded`, the thread records `Turn.reasoning` once); README speaks the host's vocabulary; **0.22.0** across every package (threads, activity, behaviour, rules, the store); Phase 25 closed — D61–D66. 1,309 tests.
- 2026-09-12 — **Phase 26: any language** (`phase-26-any-language`). **D67** the thread, the handles, the store and the composition cross the wire: `ThreadHost` a port the serving process implements; `thread/*`, `turn/*` (the turn's record as the result; events, items and activity as notifications tagged with the thread), `approvals/*` with the three answers and `{text}`, requests pushed and withdrawn, `run/cancel`, `store/*`, `modes/list`, `rules/list`; the parity invariant's rule 4 holds every public method of `Thread`, `Approvals` and `Store` to a `protocol.py` name; `shadow-hdk-serve` — the composition the coder and the studio each carried, as the harness's own (`ServeHost`, `a_thread`, `workshop`, `modes_for`; `shadow-hdk serve harness.toml --stdio|--http [--page]`); a thread's offer held by one task for its lifetime, found behind `--http`. **D68** the TypeScript client: types generated by json-schema-to-typescript (chosen over quicktype after a recorded survey — types only), one module per contract, `discriminator` stripped and a recursive root `$ref` inlined, a hand-written client over `POST /rpc` + SSE, `tsc --noEmit`, the drift invariant (measured to catch one line), proven against a live `serve --http`; CI installs node and builds it. **D69** the studio is a page `serve` itself serves, talking the wire and nothing local — held by a test that reads its source; two things the wire lacked, found by that move: `files/list` + `files/read` as thread methods under the thread's root, and a session's threads closing with it (every reload had left a provider alive); `serve` takes `--root/--mode/--store/--provider` over the file. Measured live: one turn on Claude Code through the page over the wire (7 tool calls, 31¢); a reload resumed the thread over a new session with no provider left behind. Contract change: **0.23.0** across eighteen packages. 1,338 tests; mypy strict over 233 files.
- 2026-09-12 — **Phase 27 opened: batteries and the facade** (`phase-27-batteries-and-the-facade`, cut from `main` at v0.23.0). Specs written: overview, plan (five groups), tasks, history.
- 2026-09-12 — **Phase 27 group 1 — a battery is a file (D70)**: `shadow_hdk.serve.batteries` — `Battery` documents (mcp server or python callable; tools under the harness's names with vouched effects; licence and install), `BatteryRegistry` over shipped files, a directory and a store (`store_batteries`), `open_battery` reporting what cannot run and why; `McpComponents(only=, aliases=, effects=)`; shipped `wigolo.toml` (AGPL, its own process) and `ddgs.toml` (optional `[search]` extra). wigolo surveyed live from a scratchpad install: ten tools, no annotations. Seven mutants killed.
- 2026-09-12 — **Phase 27 group 2 — batteries in the composition**: `[tools] batteries`/`dir` in `harness.toml`; `ServeHost` opens the batteries wanted once per process (each held by one task), lists them (`batteries/list`: on · off · unavailable and why) and closes them (`aclose`, called by `serve --http` and `--stdio` at the end); `workshop(batteries=)`; `a_thread(batteries=, batteries_dir=, agent=)`; the TS client's `batteries.list`. Live on wigolo through the studio in read-only: search found, one fetch blocked by bot protection and reported, the registry fetched, 27¢. Found and fixed: `serve --http` outlived SIGTERM while a page held its stream (`timeout_graceful_shutdown=2`, tested).
- 2026-09-12 — **Phase 27 group 3 — the facade (D71)**: `Harness` in `shadow-hdk-serve` — `Harness.load("harness.toml")` or `Harness(root, mode=…)`, `async with`, `turn()` yielding events, activity and items in order and the turn's record last; `[budget]` in the file is the kernel's `Lease`; `governance=`, `sink=`, `observer=`, `agent=` hand a product's port in; `.thread`/`.host`/handles underneath. Two invariants: the facade imports only public names and touches nothing private; every `harness.toml` key maps to a port or a profile. The host example stays the deep demonstration.
- 2026-09-12 — **Phase 27 groups 4–5**: the coder example is the facade (its `thread` module gone; the host example stays the deep demonstration); the README's `harness.toml` and three lines run for real by a test; `specs/architecture/optimiser.md` (D72) — the port specified, the evaluator locked first, DSPy mapped, not built.
- 2026-09-12 — **Phase 27 closed: batteries and the facade** — D70 a battery is a file · D71 one package is the front door (`Harness`, three lines, `budget`, two invariants) · D72 the optimiser specified; `architecture/overview.md` and `adapters.md` grew the front door and batteries; **0.24.0** across eighteen packages; 1,364 tests; mypy strict over 237 files.
- 2026-09-12 — **Phase 28 opened — the workspace** (owner's review + a demo outside the tree, `../harness-demo/`): group 1 — `Thread.tools()`, `tools/list`, `skills/list` (D73); the host's one skill registry across threads; the page's Tools and Skills panes; TS client; `--flag value`. Group 2 — BUG-030 a child inherits its parent's context (D74; `set_mode("read-only")` had let `run_shell` write); BUG-031 every Claude Code built-in off (`--tools ""`, D75); the `ask` mode, measured live with approve · approve-and-add-rule · deny; `shadow-hdk-serve[providers]`. Filed BUG-032 (stale catalogue after `set_mode`), BUG-033 (a battery outlives serve), ENH-009/010/011. Groups 3–4 designed: many roots per thread, added live; the environment follows the mode; minted skills kept in the store.
- 2026-09-12 — **Phase 28 closed: the workspace** (D76): `Workspace`/`Root` in the kernel; the environment over many roots (profiles, proof, `inside()` by name), `Environment.reopen`; `ThreadRecord.roots`/`.environment`; `thread/start {roots}`, `thread/add_root`, `WorkspaceChanged` (the fifteenth kind), `files/*` by root; `ModeSpec.environment` and `set_mode` re-opening the sandbox; BUG-032 — `tools/list_changed` sent, Claude Code ignores it (measured), so the provider is reopened on its own session (`AgentPort.open(resume=)`, `--resume`; measured live: memory kept, list fresh); `KeepingSink` (ENH-011); ENH-009/010 closed; BUG-033 not reproduced (measured three ways); ENH-012 filed. Measured live: a second directory added mid-conversation and written to; read-only after workspace-write denied every write. **0.25.0** across eighteen packages; 1,392 tests; mypy strict over 245 files.
- 2026-09-12 — **fix: a battery dies with the process; the provider starts from a clean scope** (BUG-033, ENH-012). `held_stdio_client` in the MCP adapter: the server's process is ours, a session leader `hold`-ed by the runtime (D53), ended with its group on close and at interpreter exit — proven against a server that ignores EOF/SIGTERM/SIGHUP with the host SIGTERMed from outside. Claude Code launched with `--setting-sources ""` and `CLAUDE_CODE_DISABLE_AUTO_MEMORY=1` (measured: a sentinel `CLAUDE.md` gone, auto-memory gone, claude.ai login kept; `--bare` rejected because it never reads the keychain); verified through the harness, 5¢.
- 2026-09-12 — **one rule, one implementation** (D77, `fix/one-rule-one-implementation`): `runtime.processes.start_held` is the one place a session leader is started (the leash, jsonl, ACP and the battery transport call it; an invariant refuses the next copy — RED named five); `runtime.lines.LineBuffer` is the one framing for newline-delimited JSON (the wire's stdio channel and the recording pipes read by it; four mutations each fail); the workspace's root-name rule replaces the `exists()` heuristic — another root's name wins, the primary's name is not an address, a clash is refused when the root is named.
- 2026-09-13 — **the specs synced to 0.25.2** (`specs/adhoc/2026-09-13-sync-specs-to-0.25`): `overview.md` (the packages as they are, the front door, the governed step's context, the thread), `wire.md` (the thread methods at D76), `adapters.md` (the map, the four modes and their environment modes, the workspace, batteries held and kept), `runtime.md` (the module rows), `testing.md` (every invariant named), `file-structure.md` regenerated from the tree, README (the ports table, fifteen event kinds, eighteen distributions at 0.25.2), the substrate's live table, the impact map. Found and fixed: BUG-034 — the mode in settings is a mode id, `ask` included; an unknown environment name is refused by `requires`.
- 2026-09-13 — **the packages as published artefacts** (0.26.0): the providers wheel built for the first time — a `force-include` had duplicated what `packages` carried and nothing had ever built a wheel (**BUG-035**); `test_every_wheel_builds_and_carries_its_files` builds all eighteen and looks inside each for the data files its package reads; a clean venv with nothing but the wheels imports `shadow_hdk.serve`, resolves the transports, and `shadow-hdk serve --stdio` answers `initialize`; the licence line is the owner's in every package.
- 2026-09-13 — **published** (0.26.1): `[project.urls]` and classifiers on every package; `.github/workflows/publish.yml` builds the eighteen, `twine check`s them, publishes to TestPyPI on demand or to PyPI on a published release (the tag must equal the packages' version), and smoke-installs from the index into a fresh venv — imports, `shadow-hdk serve --stdio` answers `initialize`. README's *Install* section. 0.26.1, a patch.
- 2026-09-13 — **one distribution** (0.27.0, D78, `specs/adhoc/2026-09-13-one-distribution`): the eighteen packages become one `shadow-hdk` with extras (`[langchain]`, `[openai]`, `[anthropic]`, `[ollama]`, `[huggingface]`, `[mqtt]`, `[otel]`, `[sandbox]`, `[search]`, `[all]`); one `src/shadow_hdk` tree, one `pyproject.toml`, the package READMEs in `docs/packages/`. mypy discovers **377** files where the namespace layout let it see 253 — 63 latent strict errors fixed at their cause, among them **BUG-036** (the OpenSandbox volume shape) and `RemoteModel.stream` a coroutine where the port is an async iterator (the four remote halves now name their ports and run the contract suites over a loopback). **BUG-037**: the registry's one funnel took the last port to offer an id — first keeps it now, the hidden offer named in `shadowed`. The gate invariant asks mypy's own discovery; the sdist is declared (284 kB, was 11 MB of `node_modules`); the README's first block was not the kernel's shape and the self-contained blocks run under a test. Clean venv with `shadow-hdk[all]` from the wheel: imports, `serve --stdio` answers `initialize`. 1,436 tests; nine mutations, all bite
- 2026-09-13 — **the first product installs the kit** (0.27.1): the React example in `shadow-hdk-demo/react-app` — `shadow-hdk[all]==0.27.0` from PyPI behind a Vite proxy, the TypeScript client by path — found two things on its first connect. **BUG-038**: the SSE stream sent nothing until asked, and a Node front holds the headers (the session id among them) until the first body byte — a deadlock; the stream opens with a comment frame now. **BUG-039**: the client called `fetch` with itself as `this`, which a browser refuses; fixed, and the Node smoke enforces the browser's rule. The client is a package (`main`, `types`, `exports`, `prepare`). 1,437 tests
- 2026-09-14 — **ENH-016, ENH-017, ENH-018 filed** from lane H's review of Intent Studio's consumption plan: `Item` has no `inputs` (a page cannot print `Read(path)` from the fold); no `AgentPort` over a `ModelPort` (a model-backed conversation cannot be a `Thread` — the product's two-path engine is the kit's gap, not its misuse); the stream session (ids · outbox · grace · replay) is locked inside `wire/serve.py` and an in-process product must rebuild it. Proposed as the next short phase: *the in-process door owes what the wire door has*
- 2026-09-14 — **ENH-014, ENH-015 filed** from the React example's 0.29.1 run: the SSE stream has no heartbeat, so a silent drop (a proxy that keeps the response open, a NAT) is invisible to the client until TCP says so — a keepalive comment frame and a client silence timeout; and `--token` on `argv` is visible in `ps` — read it from the environment or a file
- 2026-09-14 — **BUG-044 closed (0.29.1)** — the agent's own question parks too: `ask_person` answered `park` had taken the text `Parked()` as the person's words and closed the question; now `request_input` hands back `Parked` (in-process and across the wire), the component refuses *not now* naming the question, and the question stays open through its step's observation — the turn ends `parked` with it on the record, and `settle` folds the person's text ahead of the next prompt. Found by the React example while wiring D88 to an input card. RED first in the runtime and over the wire; two mutations bite
- 2026-09-15 — **Epic 0008 production-boundary approved and planned**: one Shadow with two surfaces (Shadow HDK, the construction kit; Shadow Harness, the ready-made public-contract assembly); Phases 31–33 form one strict test-first v0.30.0 release train — honest capability/requirement matching, one durable agent surface, and current-authority crash-safe effects. Intent Studio pauses new execution integration until that release; its product-owned cloud/domain work remains outside the HDK. D95–D106 settled; ENH-019, TD-010 and TD-011 filed; the charter, success criteria, roadmap, status and impact map brought to the same plan.
- 2026-09-15 — **Phase 31 started — a host knows what it can trust** (`phase-31-a-host-knows-what-it-can-trust`): derived from Epic 0008 with no second interview; capability, requirement and evidence are separate typed values; providers and environments must report conservative facts; one compatibility decision reaches Python, Harness, serve and TypeScript before either side is opened.
- 2026-09-15 — **Phase 31 group 0 — the capability algebra**: strict TDD locks provider and environment capabilities, caller execution requirements, per-axis evidence and stable mismatches; unknown is explicit and never satisfies a production requirement. Focused kernel/provider gates: 32 tests, ruff and mypy green.
- 2026-09-15 — **Phase 31 group 1 — providers tell the measured truth**: provider TOML carries validated capabilities and evidence; detection exposes them unchanged. Claude Code records a controlled path, Codex records its uncontrolled native/configured paths, and OpenCode leaves unmeasured facts unknown. Model-provider records use the same seam. Provider/kernel gates: 90 tests, ruff and mypy green.
- 2026-09-15 — **Phase 31 group 2 — environments tell the proven truth**: `Isolation × mode` now yields typed environment capabilities; secret posture distinguishes denied, ambient and unknown. Stricter requirements refuse with the complete typed mismatch before use. Live macOS proof reports workspace-confined writes and denied network without claiming confined reads or secret denial. Environment gates: 83 tests plus the focused 12-pass live file, ruff and mypy green.
- 2026-09-15 — **Phase 31 group 3 — one selection decision on every door**: requirements cross `a_thread`, `Harness`, `ServeHost` and `thread/start`; the selected facts return in Python and protocol v2, persist as thread-record v3 requirements for resume, and fail with identical typed mismatches before an agent opens. `providers/list` and `capabilities/check` expose discovery/proof; schemas and the TypeScript client regenerated. BUG-045 fixed the schema publisher's escaped default target. Serve/wire gate: 229 tests, ruff, mypy and TypeScript green.
- 2026-09-15 — **Phase 31 group 4 — capability contract published**: the provider matrix, effective environment truth, protocol-2 boundary and 0.29.1 migration are synchronized across architecture, package and consumer docs. The text preserves unknowns and says the measured macOS boundary precisely: workspace-confined writes and denied network do not imply confined reads or denied ambient secrets. Focused document/parity/schema gate: 53 tests.
- 2026-09-15 — **BUG-046 closed at the Phase 31 gate**: the configured build command now installs all packages and all extras, rather than successfully uninstalling the optional dependencies required by full pytest/mypy collection. The gate also found and formatted five Phase 31 files before completion evidence was recorded.
- 2026-09-15 — **BUG-047 closed at the Phase 31 gate**: two pre-existing OKF metadata violations were repaired without changing their historical content — the TD-009 pull-request body is an artifact, and the reserved decisions index carries no frontmatter.
- 2026-09-15 — **Phase 31 complete, unreleased Epic 0008 checkpoint**: typed capability/evidence/requirement contracts, measured provider and effective-environment truth, one selection decision on every construction door, protocol 2 and the 0.30 migration contract are pushed. Final gate: build with all extras; Ruff lint/format; mypy strict over 422 files; 1,600 tests passed, 12 skipped and 12 live deselected; 55 explicit document/decision/schema/benchmark checks; OKF over 185 specs. Phase 32 and 33 plans are fully derived. No tag, protected-branch merge, release or Intent Studio edit occurred.
- 2026-09-15 — **BUG-048 closed at the Phase 31 spec sync**: the decision-index invariant now recognizes epic decision-table rows as declarations, so D95–D106 are indexed without duplicating their canonical Epic 0008 record.
- 2026-09-15 — **Phase 32 started — one agent surface** on stacked branch `phase-32-one-agent-surface` from verified Phase 31 commit `7b9f298`. Strict Group 0 RED comes first: one model/CLI lifecycle evaluator, Item.inputs, the reusable stream-session state machine and bearer-source adversarial cases. No interim release.
- 2026-09-15 — **Phase 32 Group 0 evaluator is RED**: product-facing tests lock one Thread/tool/usage/activity surface for CLI and model agency, canonical bounded Item.inputs, a deterministic bounded/single-attachment stream session with typed stale cursors and ephemeral heartbeats, and safe bearer-source precedence/permissions/redaction. BUG-049 records that the current HTTP live path yields each numbered frame twice; the shared-session extraction must make replay and live delivery exactly once.
- 2026-09-15 — **Phase 32 model cancellation evaluator is RED**: interrupting a Thread around a blocked ModelPort currently marks the run for cancellation but leaves the provider call blocked; the adapter must cancel its active call and let the ordinary Thread lifecycle record the turn as cancelled.
- 2026-09-15 — **Phase 32 Group 1 — one agent surface**: `ModelAgent` adapts a caller-owned ModelPort below the existing AgentPort and Thread lifecycle; Python and Harness accept `model=` through the Phase 31 capability gate. Model and CLI providers now share governed tools, items, spend, activity, parked-child settlement, resume and interruption. Focused evidence: 187 lifecycle tests, strict mypy/Ruff, and destructive mutations for model routing, cancellation and parked-child identity.
- 2026-09-15 — **Phase 32 Group 2 — the item says what was invoked**: `Item.inputs` is folded from `Invoked`, round-trips through Python/wire/schema/generated TypeScript, and becomes an explicit byte-counted omission marker above 64 KiB of canonical JSON. Focused evidence: 67 checks, strict mypy/Ruff, TypeScript build, schema/client drift, and three destructive mutations for retention, the exact bound and wire propagation.
- 2026-09-15 — **Phase 32 Group 3 — one reusable stream session**: runtime StreamSession owns monotone bounded replay, typed stale cursors, one attachment, injected-clock grace expiry and ephemeral heartbeat frames; HTTP/SSE now consumes it. Twenty-two focused state/property and D94 integration tests, strict mypy/Ruff and four destructive mutations are green. BUG-049 is deprecated as an invalid code reading: its two yield sites were replay then live, not a duplicated live emission.
- 2026-09-15 — **Phase 32 heartbeat/silence evaluator is RED**: the HTTP surface has no heartbeat configuration and the generated client has no silence deadline. A live SSE test and deterministic TypeScript fake transport now require an ephemeral heartbeat and cursor-preserving reattach when a link stays open without bytes.
- 2026-09-15 — **Phase 32 Group 4 — silent links and safer bearers**: StreamSession-backed HTTP emits ephemeral 15-second heartbeat comments; the TypeScript client has a 45-second configurable silence deadline and reattaches with its cursor. Serve bearer precedence is permission-checked token file, `SHADOW_HDK_TOKEN`, then the documented local-only flag; ambiguous, linked, insecure, multiline and disclosing file paths are refused. Twenty focused checks, strict mypy/Ruff, TypeScript build/smoke and five destructive mutations are green; ENH-014–018 are closed by Groups 1–4.
- 2026-09-15 — **Phase 32 contract synchronized**: architecture, migration and package docs now publish one model/CLI Thread surface, bounded `Item.inputs`, reusable reconnect state, heartbeat/silence recovery and safe bearer precedence. `momentum okf index` correctly refreshed the phase index but attempted to erase the custom D1–D106 decision map; that output was rejected and tracked as **BUG-050**.
- 2026-09-15 — **Phase 32 complete, unreleased Epic 0008 checkpoint**: model APIs and resident CLIs now meet behind one durable Thread surface; items expose bounded inputs; reconnect state is a reusable runtime primitive; HTTP heartbeat/client silence recovery and safe bearer sources are shipped on the branch. Final gate: sdist/wheel, Ruff lint/format, mypy strict over 429 files, 1,625 tests passed with 12 skips and 12 live deselections, 61 document/schema/parity checks, 30 focused lifecycle/benchmark checks, TypeScript check/build/silence smoke and OKF over 185 specs. BUG-051 and TD-012 record non-blocking cleanup warnings. No tag, protected merge, release or Intent Studio edit occurred.
- 2026-09-15 — **Phase 33 started — authority at the act** on stacked branch `phase-33-authority-at-the-act` from verified Phase 32 commit `2ab153a`. No P0 bug blocks it; TD-010 and TD-011 are its planned P1 boundary. Strict Group 0 freezes canonical authority/effect digests, legal journal transitions, single-use grants and the crash/recovery matrix before implementation.
- 2026-09-15 — **Phase 33 Group 0 evaluator is RED and frozen**: versioned corpus locks legal/illegal append order and recovery at every crash boundary; runtime tests bind authority, exact effect, grant expiry/key/run/step, stale rereads, concurrent duplicates, child replay and idempotent reconciliation. Collection fails on the deliberately absent authority/runtime contracts. A destructive hash mutation proved the corpus freeze assertion bites.
- 2026-09-15 — **Phase 33 Group 1 — authority and effect records are data**: kernel contracts define secret-free revision snapshots, exact staged-effect digests, bound single-use grants and append-only entries; host authority/authorizer/journal protocols and importable product contract suites ship. Twenty-four JSON Schemas and generated TypeScript contracts build; five focused checks, Ruff and strict mypy pass.
- 2026-09-15 — **Phase 33 Group 2 — the journal survives its process**: one legal-state fold backs compare-and-append in-memory, SQLite and Postgres journals. Durable adapters serialize writers and enforce authorization uniqueness without overwriting a fact; eight local contract/restart/concurrency tests pass and eleven real-Postgres checks collect behind the existing environment gate. A destructive CAS mutation produced four failures.
- 2026-09-15 — **Phase 33 Group 3 — authority is checked at the act**: every controlled irreversible invocation now stages after governance/approval, obtains a grant bound to the exact act, rereads current authority, records executing, invokes once and journals its receipt or failure. Unsupported controlled paths fail closed; observed paths remain observational. The frozen evaluator (29), focused integration (4), full runtime suite (448 pass, 1 skip), Ruff and strict mypy are green; destructive mutations prove both the act-time reread and fail-closed boundary.
- 2026-09-15 — **Phase 33 Group 4 — recovery without blind retry**: the store URL now selects a durable effect journal, and ServeHost/workshop supply live reference authority plus exact-stage authorizations. Terminal evidence survives a SQLite host restart and is reused only for the identical stage; unfinished non-idempotent execution becomes explicit unknown and reconciliation requires declared idempotency plus external evidence. Child grants remain run/step-bound and shipped controlled adapters cross the common boundary. The combined gate passed 829 tests with three skips and one deliberate deselection. **BUG-052** fixed SQL journals rejecting their own legal transition because an authorization ID had been copied beyond its single consumption fact.
- 2026-09-15 — **Epic 0008 v0.30.0 candidate complete**: Phases 31–33 now form one verified production boundary: evidence-backed capability selection, one model/CLI durable agent surface, and act-time authority with append-only crash-safe effect records. `effect_recorded` crosses Python, wire/schema and generated TypeScript; OpenTelemetry keeps only safe transaction metadata; protocol 3 refuses older peers. Final non-live gate: 1,688 passed, 14 skipped, 12 live deselected; build, Ruff, strict mypy (441 files), TypeScript, Twine and OKF all green. The candidate is pushed but intentionally awaits protected merge/tag/publish approval; Intent Studio remains untouched.
- 2026-09-15 — **BUG-053 closed during v0.30.0 release preparation**: the publish smoke had hard-coded protocol 1. It now obtains `PROTOCOL_VERSION` from the fresh installed artifact, so package releases remain correct when the wire vocabulary changes independently.
- 2026-09-15 — **v0.30.0 released**: Epic 0008 lands Phases 31–33 together — capability evidence and requirements, one model/CLI Thread surface, and revisioned current-authority effect transactions with durable receipts or explicit unknown outcomes. The release is protocol 3; a GitHub release triggers PyPI publishing and its fresh-install smoke.
- 2026-09-14 — **Phase 30 group 6 — sessions that idle out; a stream that survives a drop (D94)**: `idle_seconds` on `Conversation`, `Thread` and `[provider]`; the wire's frame `id:`s, a session outliving its stream for `grace_seconds`, reattach with `Last-Event-ID` and replay, 409 on a second stream, 404 past the grace; the TypeScript client reattaches (`reconnectSeconds`, `onStream`).
- 2026-09-14 — **Phase 30 group 5 — a parked run behind our port; the record versioned (D93)**: `kernel.RunStore`, `runtime.checkpoints.saver_over` / `InMemoryRunStore`, `ServeHost(run_store=)`, `RunStoreContract`; `ThreadRecord.version` (`RECORD_VERSION = 2`; an older record written back). **Migration note:** a rule written before 0.28.0 has no `scope` and is for everyone.
- 2026-09-14 — **Phase 30 group 4 — routed governance; typed refusals (D92)**: `adapters.modes.Routed`; `wire.protocol.ERROR_KINDS`, `error.data = {kind, …}` on every refusal, the TypeScript client's `RemoteError` with `kind` and `detail`.
- 2026-09-14 — **Phase 30 group 3 — the contracts ship; a `Questions` port (D91)**: `shadow_hdk.testing` (`contracts`, `providers.ScriptedAgent`, the doubles); `kernel.Questions` and `kernel.Request`; `RunOptions.approvals: Questions`; `QuestionsContract`.
- 2026-09-14 — **Phase 30 group 2 — tokens and running time on the record (D90)**: `Spent.input_tokens`, `.output_tokens`, `.unmetered`; `LeaseMeter.count_tokens`, `.pause`/`.unpause`; a thread's seconds are its turns' running time, not its life.
- 2026-09-14 — **Phase 30 opened — a product owns what it owns** (`phase-30-a-product-owns-what-it-owns`, from the research note *what a harness development kit owes its products* and the owner's go). **Group 1 — the governed turn (D87), a park on purpose (D88), the agent streams (D89)**: `runtime.Conversation` — one provider session, its turns governed and streamed, `last: Turned`, no record; `Thread` over it, every test unchanged; `Parked` as an answer (`approvals/answer {kind: "park"}`), `turn(on_question="park")` / `turn/start {on_question}`, the turn ends `parked` with the question kept and `settle` runs the act later; `AgentComponent` streams through `ModelPort.stream` as activity, the response assembled; `ModelChunk.reasoning` on the default, the double and the remote; `LangChainModel.stream` merges chunks and keeps thinking (ENH-065 closed).
- 2026-09-14 — **Phase 29 group 7 — operations (D86)**: `GET /healthz` (no bearer), `initialize → version`, `admin/sessions`, `admin/threads`; the per-run token of BUG-006 closed as a decision — one bearer held by the product's backend, the identity on the thread.
- 2026-09-14 — **Phase 29 group 6 — the rules the field has (D85)**: `ActRule.decision = "ask"`; governance reads deny → ceiling → ask → mode → allow, so deny and ask rules hold in `full`; the strongest matching rule decides (`ActRules.decide/decide_now`, `strongest`); `kernel.rules.pattern_matches` — `**`, `*`, `?`, `[…]`, a trailing `*` as the prefix it always was — over a rule's inputs.
- 2026-09-14 — **Phase 29 group 5 — the budget on the record (D84, ENH-013 closed)**: `thread/start {budget}`; `ThreadRecord.budget` (`Ceiling | None`) and `.spent` (`Spent`: steps, seconds, cents, unpriced) saved after every turn; a resumed thread's meter starts from the record; `thread/remaining` is budget − spent across resumes.
- 2026-09-14 — **Phase 29 group 4 — batteries live (D83)**: the store's `wanted` rows (`{id, on}`), seeded once from `[tools] batteries`, decide which batteries the next thread gets; `ServeHost` opens and closes them at every thread's open; `batteries/list` by the rows.
- 2026-09-14 — **Phase 29 group 3 — identity on the thread, scope on the rows (D82)**: `thread/start {principal, attributes}` → the record and every judgement's context (children carry the principal; `RunContext.principal`); `ActRule.scope`, `ModeSpec.scope`, `kernel.rules.in_scope`; `ModeGovernance` in scope; `ActRules.all_now` / `ModeRegistry.find/all` take the identity; `rules/list` and `modes/list {thread_id}`; `set_mode` refuses out of scope; a card's rule scoped to the answerer; `adapters.md` names the two ways to keep tenants apart.
- 2026-09-14 — **Phase 29 group 2 — one thread, one holder (D81)**: `ThreadStore.hold/renew/release/held_by` (memory, sqlite, Postgres; the contract suite says what a hold means); `Thread.open/resume(holder=)` take, renew and release it — `ThreadHeld` names the holder; `ServeHost` is `host:pid:nonce`; `thread/list` rows carry `held_by`; `turn/start {when: enqueue | reject | interrupt}` (`TurnRunning` names the running turn). **BUG-042** (a second turn refused *no steps left* while one ran) found and closed.
- 2026-09-14 — **Phase 29 opened — one app server behind every surface** (`phase-29-one-app-server`, from the research note `specs/research/2026-09-14-how-comparable-runtimes-do-it.md` and the owner's decision). **Group 1 — the record chooses its store (D79); a parked turn survives the host (D80)**: `[store] url` (`sqlite:///…` | `postgresql://…`; `path` as sugar) → `serve.stores_for` — the Store, the ThreadStore and the checkpointer from one choice; `adapters.postgres` (`PostgresStore`, `PostgresThreads`, LangGraph's Postgres saver) behind `[postgres]`, the contract suites against a real server under `SHADOW_HDK_TEST_POSTGRES_URL` (the desk's Postgres 16; CI's service); `ServeHost(store=, threads=, checkpointer=)`; every served thread and every wire session on the host's checkpointer (`langgraph-checkpoint-sqlite` a base dependency); `ThreadRecord.pending` (`PendingQuestion`) kept as questions open and close; a turn the host died in is `parked` (a question open) or `cancelled`; `thread/resume → pending` and the questions pushed again; `approvals/answer` on a left handle is `Thread.settle` — the parked child run resumed from the checkpointer, the agent told at its next turn. **BUG-041** (a cancelled reader of `run()` hung on a drive waiting for nobody) found and closed.
- 2026-09-13 — **the approval cards** (0.27.2): the React example's chapter 3 found **BUG-040** — a tool call the mode asks about left an empty item (the live ask named step `""`) and, answered, closed its own step a second time without its component. The offer names the call's step; the fold reopens a step that closed on a question as the same step, with what its invocation said. The publish smoke waits up to ten minutes for the index. 1,439 tests
- 2026-09-17 — **ENH-020 filed** (P2): a mode's `system`/`model`/`effort` is silently dropped on a provider whose record maps no `behaviour_args` (Codex, OpenCode); `unmapped_behaviour` exists but nothing in the runtime calls it. Found while writing the subscription-CLI guide.
- 2026-09-18 — **Epic 0009 — the harness as data — planned** (`specs/epics/0009-the-harness-as-data.md`): D107–D121 settled once; Phases 36 (plan admission, deps 33) → 34 (the harness as data, deps 36) → 37 (the durable run request, deps 33, 34); `release: per-phase`, `tdd: strict`. Opened from the research note `2026-09-18-what-belongs-in-the-kit.md`. The roadmap re-derived (D117): 36 no longer depends on 34/35; 34 reframed for the blueprint layer; 35 after the epic. TD-010/011 closed — shipped by Phase 33 Groups 3–4, the rows had lagged the release. The status header's stale "built but not yet published" sentence corrected.
- 2026-09-18 — **Phase 36 started — plan admission** (`phase-36-plan-admission`, Epic 0009, deps 33): the frozen admission corpus first (G0), then the kernel's `PlanLimits`/`admit()`/plan events, admission inside `spawn`, `compose` as a registered component, limits on the mode, run-after-planner and amend, ENH-020 folded in as G6, wire/TS parity and v0.31.0. Pre-phase check: no open P0/P1; `momentum learnings` names TD-012 as a possible stale closure (advisory, P3, not flipped). `momentum okf index` again rewrote the decisions map (BUG-050) — restored from a copy.
- 2026-09-18 — **Phase 36 G0** (`4dd15a1`): the admission corpus v1 frozen with its digest pinned; kernel and runtime RED scenarios; `PlanLimits` narrowed to what admission can measure — depth, fan-out, steps.
- 2026-09-18 — **Phase 36 G1** (`f13826c`): `kernel/planning.py` — `PlanLimits`/`meet`, `measure()`, `admit()`, `composition_digest()`; `plan_admitted`/`plan_refused` in the `Event` union (eighteen kinds); four contracts published; TypeScript regenerated; three mutations bite.
- 2026-09-18 — **Phase 36 G2** (`7705131`): admission inside `children.spawn` — structure and existence refused before compile, effects dry-judged in the child's context and *named* (`asks`, `refusals`) rather than pre-empted (D108/D121 amended, recorded in the epic); `RunOptions.plan_limits`, `Pattern.plan`, `Pattern.absorb` (data); `PlanNotAdmitted` to the caller; the loop, the `spawn` helper verb and the offer adapted; `Item.plan`; OTel; the wire carries limits and crosses `plan_refused`. **BUG-054** filed: two README snippet tests fail on `main` since the v0.30 README refresh.
- 2026-09-18 — **Phase 36 G3**: `compose` is a registered component (`runtime/planning.py`, D110) — offered like any tool, no effects, the composition as input, every result as output, held to the component contract; the loop's meta-tool and the component share one path into `spawn`; `single` stays unable to plan; the offer gives a plan proposal the parent's remaining steps. Measured live on Codex CLI 0.154.0 through the socket (Claude Code signed out here): two admissions on the record, both `read_file` steps through the registry, 2 turns, 57,471/324 tokens — the first live proof of the relay end to end on Codex.
- 2026-09-18 — **Phase 36 G4**: plan limits on the mode (D109) — `ModeSpec.plan`, the `[plan]` table (malformed refused; unset axes inherit the policy's; wider than the policy refused by name), shipped defaults `PLAN_OF` narrowing from `full` to `read-only`, `widens_plan()`, `Conversation.plan_limits` = host `meet` mode at every turn, `Thread.open/resume(plan_limits=)`; a mode switch changes the next plan's limits live.
- 2026-09-18 — **Phase 36 G5**: `RunState.plan` carries the composition through the checkpoint; a settle resumes on the shape it parked with (`parked_composition`); `resume` admits a different composition as an amendment (D116) — `Amend` is an answer kind, `Thread.amend` a settle with it, `Children.amend` wakes the held plan on the new shape or leaves it parked; `compose` parks on a question raised inside its plan (D57) and re-parks on a refused amendment; `Pattern.absorb=False` defers an admitted plan to run after its planner (D112). A settle reserves the thread's remaining budget so a nested plan is not starved.
- 2026-09-18 — **Phase 36 G6**: unmapped behaviour is named, not dropped (ENH-020 closed) — `AgentSession.unmapped`, `Thread.unmapped_behaviour`, `unmapped_behaviour` on the thread results and in the TypeScript client; `unmapped_behaviour` moved to `kernel.providers`.
- 2026-09-18 — **Phase 36 G7**: the plan crosses the wire — `thread/amend`, `plan_limits` on `thread/start`/`resume`/`set_mode`, `plan` on `modes/list`, the TypeScript client typed (protocol 3 unchanged, additive); BUG-055 found and closed (a second park in one leg replayed the first answer — the executor drains replays, `Thread.settle` keeps the renewed question); BUG-054 closed (the README's snippets run as printed); `docs/migrations/0.31.md`, the package guides, `consuming.md`; `codex.toml` says the relay is proven; version 0.31.0 at the gate.
- 2026-09-18 — **v0.31.0 released** — Phase 36 **plan admission** (Epic 0009's first phase, D107–D121): `PlanLimits` and `admit()` pure in the kernel; admission inside `children.spawn`, true for every caller on either side of the wire; `plan_admitted`/`plan_refused` on the record and in the fold; `compose` a registered component — a resident CLI planned through the socket, measured live on Codex CLI 0.154.0; plan limits live on the mode, narrowing only; a plan may run after its planner; `thread/amend` for a parked plan; `unmapped_behaviour` named rather than dropped (ENH-020 closed). BUG-054 and BUG-055 closed. Protocol 3 unchanged — every addition is a method or a field. 1,773 non-live passed; mypy 452 files; the 0.31.0 wheel installed fresh and answered `initialize`. ENH-021 carried out: the demo's re-pin follows the publish.
- 2026-09-19 — **docs: for a product** — `docs/for-a-product.md` answers lane P's ten questions (artifacts as an act, modes and behaviour, questions across a process death, delegation, capability inventory, the trail's facts, the served runtime, session resume, spend, composition) against 0.31.0 with a composition example for both doors; five follow-ups filed (ENH-022 `components=` on the facades, ENH-023 cache tokens on `Usage`, ENH-024 a typed `session_gone`, ENH-025 a capability-inventory axis, ENH-026 the chapter kept true).
- 2026-09-19 — from lane P's context, verified against the code: **BUG-056** (`set_mode` mid-turn closes the session under the turn, P1), **ENH-027** (OpenCode loses memory on a mode change silently; `resumed` on the result), **ENH-028** (Codex `model`/`effort` mappings measured). A suspected wire bug — a delegated step's live question crossing without its thread id — was tested and is not one: the offer lifts the question onto the turn's run.
- 2026-09-19 — ENH-022 withdrawn under the admission rule (a product's tools reach a served thread as `python`/`mcp` batteries or through the `Thread` door — two doors, not a gap); `docs/for-a-product.md` question 7 and the composition section corrected to say so.
- 2026-09-19 — filed from the multi-language question: **ENH-030** the thread door's inversion for a wire host's components (Phase 34), **ENH-031** the TypeScript host-side stub and pinned sidecar (Phase 34), **ENH-029** a `python` battery naming a module of callables, **ENH-032** Python 3.13/3.14 in the matrix (the tree resolves on 3.14 with every extra, dry-run).
- 2026-09-19 — **BUG-057** found by running the full suite on Python 3.14.6 (uv's standalone build): the seatbelt proof's `WROTE` marker is echoed by 3.13+'s tracebacks, so a denied write reads as a write and every confined mode refuses to open; 55 failures from one line, 1,718 passed. The three probes pass by hand on 3.14. Blocks ENH-032.
