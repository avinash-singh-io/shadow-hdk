[project]
name = "shadow-hdk-adapters-contained"
version = "0.14.0"
description = "A sandbox that proves it is contained, or refuses to exist."
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
license-files = ["LICENSE"]
dependencies = ["shadow-hdk-kernel==0.14.0", "shadow-hdk==0.14.0"]

[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/shadow_hdk"]
