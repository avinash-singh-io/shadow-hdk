# shadow-hdk-adapters-contained

A sandbox that says `contained: true` only after proving it (D25). A backend proves itself with
something true inside it and false on the host; a proof that fails refuses construction.
